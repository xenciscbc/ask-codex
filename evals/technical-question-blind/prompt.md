---
description: "Technical question: the question reaches Codex, the user's leaning does not."
max_turns: 30
timeout_seconds: 900
allowed_tools: [Skill, Bash, Read, Glob, Grep, Write]
---

/ask-codex:ask What is the difference between an AbortController-based timeout and a library timeout option for fetch calls? I think AbortController is always better.
