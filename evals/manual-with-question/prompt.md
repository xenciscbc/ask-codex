---
description: Manual consultation with an explicit question — default MCP policy, full invocation contract, dispositions presented.
max_turns: 30
timeout_seconds: 900
allowed_tools: [Skill, Bash, Read, Glob, Grep, Write]
---

/ask-codex:ask Why does fetchUser in src/user.js return an empty object when the API times out, and is that what causes the profile page to show "user not found"?
