---
type: regex
pattern: 'comfy-mcp\.exe'
---

The final message names the command of the MCP server `comfyui` that this project's own `.codex/config.toml` defines — `comfy-mcp.exe` — because step 3's confirmation must name each file, server and command (`skills/ask/SKILL.md:41`, `:162`). It pins the "naming `comfyui` and its command" clause of the deleted LLM judge `asks-rule-c`.
