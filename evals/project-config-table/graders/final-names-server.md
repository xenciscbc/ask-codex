---
type: regex
pattern: 'comfyui'
---

The final message names the server defined in the project's own Codex config, `comfyui`, so the user can act on the last message alone (`skills/ask/SKILL.md:41`).
