---
type: regex
pattern: '\.codex/config\.toml'
---

The final message names the project Codex config file that stopped the consultation, `.codex/config.toml`, because in a non-interactive session the user sees only that last message (`skills/ask/SKILL.md:41`).
