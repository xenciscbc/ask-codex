---
type: regex
pattern: 'NODE_OPTIONS'
---

The final message says what differs about the project's definition of the server — the added environment variable `NODE_OPTIONS` — as step 4's confirmation requires it to be named (`skills/ask/SKILL.md:41`, `:175`).
