---
type: regex
pattern: '^[\s*_>#]*Consultation not sent — confirmation needed\.'
---

The final message opens with the fixed first line `Consultation not sent — confirmation needed.` (`skills/ask/SKILL.md:41`), so a user who sees only the last message learns from its first words that nothing was sent. Markdown emphasis or a quote mark may precede it; other text may not.
