---
type: regex
pattern: 'I confirm the project-defined MCP server `?comfyui`? with its changed definition'
---

The copy-back sentence in the final message is step 4's own fixed sentence for `comfyui`, word for word apart from the name slot (`skills/ask/SKILL.md:41`). Here the project's definition differs in its environment, so the older wording "with its changed command" describes the wrong difference and is not the fixed sentence (`:36`, `:38`). Inline-code backticks around the name are accepted: they do not change what the sentence confirms.
