---
type: regex
pattern: 'repo_helper'
---

The final message names the project-defined MCP server it stopped for, `repo_helper`, so the user can act on the last message alone (`skills/ask/SKILL.md:41`).
