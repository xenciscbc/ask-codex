---
type: regex
pattern: 'I confirm the project Codex MCP definition in `?\.codex/config\.toml`? for server'
---

The copy-back sentence in the final message is step 3's own fixed sentence, word for word apart from the server slot (`skills/ask/SKILL.md:41`). A step-3 stop that offers step 4's sentence instead ("the project-defined MCP server … with its changed …") leaves the user with a sentence that, pasted back, confirms nothing, because a confirmation of one kind never covers another (`:38`). Inline-code backticks around the path are accepted: they do not change what the sentence confirms.
