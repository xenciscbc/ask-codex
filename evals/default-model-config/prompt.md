---
description: "No model named: the Codex-configured model with effort medium; the configured effort low never reaches argv."
max_turns: 30
timeout_seconds: 900
allowed_tools: [Skill, Bash, Read, Glob, Grep, Write]
---

/ask-codex:ask Why does fetchUser in src/user.js return an empty object when the API times out?
