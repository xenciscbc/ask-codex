---
type: regex
pattern: '\.codex/config\.toml'
---

After a decline the final message names the file the declined definitions live in, `.codex/config.toml` (`skills/ask/SKILL.md:39` — "naming the file and the servers" — and `:162`, which already required a declined stop to name the definitions), because in a non-interactive session the user sees only that last message.
