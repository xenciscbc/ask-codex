---
type: regex
pattern: 'pencil'
---

The final message names the server this project's config actually allows, `pencil` — not the one the user's sentence named — so the user can act on the last message alone (`skills/ask/SKILL.md:41`).
