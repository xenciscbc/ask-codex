---
description: "Pre-confirmation names comfyui but the project config allows pencil: still ask, no exec."
max_turns: 30
timeout_seconds: 900
allowed_tools: [Skill, Bash, Read, Glob, Grep, Write]
---

/ask-codex:ask Why does fetchUser in src/user.js return an empty object when the API times out? I confirm this project's ask-codex config .claude/ask-codex.local.json, which allows the MCP server comfyui.
