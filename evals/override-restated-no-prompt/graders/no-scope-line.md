---
type: regex
pattern: 'Model choice applies to this consultation only'
match: not_contains
---

The named model equals the session setting already in force, so the fixed model-scope line must not appear in the final response.
