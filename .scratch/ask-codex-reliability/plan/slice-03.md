# Slice 03 — Stop path through the shipped scripts (revision 3, closing)

Ticket: `.scratch/ask-codex-reliability/issues/03-stop-path-in-skill-and-evals.md`. Spec: `.scratch/ask-codex-reliability/spec.md` (stories 4–7, 11, 19; 「停止報告固定欄位」; 「並行檢查資訊的落點」; 「停止順序」). Blocked by 01 (resolved, `ef4524b`) and 02 (resolved, `c198a37`). Mode AUTO (approved with the programme). No Claude eval budget cap; **0 live Codex calls** (live proof is ticket 04).

Revision 2 answered round 1 (REVISE, 4 blockers): trace graders anchored to the assistant's own line with a tool-result-only negative sample; the Parallel reply paragraph in scope; the `exec-finished` criterion dropped with reason and the verifier claim narrowed; one owner per ticket Status. Revision 3 answers round 2 (REVISE, 2 blockers) and is the closing epoch: (1) step 11 is in scope with one stop order — `stop.sh` while `<tmp>` exists → stop line + `TaskStop` → cleanup → final reply — so the "clean up before any stop message" rule gains the single exception it needs without weakening ticket 13; (2) ticket 15's criterion 1 (which names the deleted grader) is struck through and replaced the same way ticket 03's was. Advisories taken: unlisted graders untouched (A1); the regexes are checked against the reachability run's real trace before the remaining runs (A2); a `used-run-script` grader (A3); the `ended`-only tightening recorded in ticket 03's Comments (A4); ticket 03's "Grader 改動" paragraph corrected (A5); step 10 in a parallel stop lists both the restated notices and one `Parallel check:` line per check, duplication accepted because the spec asks for both (A6); ticket 03's own Status is resolved by this slice after the outcome verifier's CONFIRMED (A7).

## Evidence this slice rests on

- **Probes A/B (evidence/01):** after `TaskStop` the whole `sh → node → codex.exe` chain runs to completion in both the interactive and the headless session. The fix is unconditional; ticket 12's "headless only" scope is wrong.
- **Probe D (three kept traces):** the `Consultation stopped:` line was never written at the stop; it was improvised once in the final message and 2 of 3 improvisations were judged incomplete. Remedy: the line comes from the script and is written twice (at the stop, restated in step 10).
- **Ticket 15's failing run:** the check happened in a message with no text at all; fixed wording that must precede an action is unreliable, so the parallel information moves into text the model necessarily writes.
- **Trace composition (probe D traces, read 2026-09-18):** `trace.jsonl` holds one JSON object per line; the model's messages are lines containing `"role":"assistant"` (text blocks and tool_use blocks), tool results are lines containing `"role":"user"` with `tool_result` blocks. A regex that requires `"role":"assistant"` and the fixed words **on the same line** therefore matches only what the model wrote. The skill body is not in the trace (ticket 15's run: `Parallel check:` "pattern not found in trace" although SKILL.md contains it). `"name":"TaskStop"` appears only in the real call (precedent: `check-line-before-stop.md`).

## Outcome

1. **Launch through `run.sh`.** Step 8's command becomes `bash '<skill directory>/scripts/run.sh' '<tmp>' -- codex exec … - < '<tmp>/prompt.md' > '<tmp>/events.jsonl' 2> '<tmp>/stderr.log'`, still one line, still `run_in_background: true`; the `codex exec` part and its redirections are unchanged. The `pid` file lands in the run directory; step 11 removes it with the directory.
2. **Stop through `stop.sh`, then `TaskStop`.** The stop path is: (a) run `bash '<skill directory>/scripts/stop.sh' '<tmp>' --interval <T> --interval-source <default|override> --recommended <wait|stop>` (in a parallel consultation add `--done '<full slugs or none>' --still-running '<full slugs>'`, and run it once per run being stopped); (b) write the `Consultation stopped:` line the script printed, **verbatim, as the first line of the message in which you call `TaskStop`** (markdown emphasis around the fixed words is allowed, as for the other fixed lines; the words, order and values are not); (c) call `TaskStop` on that run's task whether or not the task already ended; (d) clean up (step 11) — **`stop.sh` needs the run directory (`pid`, `events.jsonl`), so the order on a stop is fixed: `stop.sh` → stop line + `TaskStop` → `rm -rf -- '<tmp>'` → final reply**; (e) restate the same line word for word in step 10 item 1. When the script exits non-zero its line ends `process tree NOT confirmed — pids <…>`: copy it unchanged and tell the user the stop could not be confirmed and which pids are listed; never rewrite it to "ended".
3. **Parallel information travels with text the model must write** (spec 「並行檢查資訊的落點」, strings verbatim). The pre-action rule is deleted. In a parallel consultation: the confirmed-alive notice is `Codex still running — <elapsed> elapsed; done — <slugs or none>; still running — <slugs>; waiting another <T> minutes.`; the `AskUserQuestion` question body's first line is `Parallel check: done — …; still running — …`; the stop report carries `; done — …; still running — …` (printed by the script from the arguments). Liveness judgement is unchanged.
4. **Parallel reply (step 10) after a stop.** Item 1 keeps the existing restatement of every timer line (override line, each `Codex still running — …` notice as written), and adds, on its own line each, one `Parallel check: done — <slugs or none>; still running — <slugs>.` line **per liveness check that ran** (composed from that check's fields, in check order — the spec asks for these lines in step 10 even though the notices now carry the same fields) and every `Consultation stopped:` line copied verbatim from step 8 (one per stopped run). The surviving run is presented normally and the `Failed model: <slug> — stopped after <elapsed> without progress` line stays, with `<elapsed>` taken from the `elapsed` field of that run's stop line so the two never disagree. Nothing is attributed to the stopped model.
5. **Ticket bookkeeping.** Tickets 15 and 16 are resolved by this slice (Comments with the `--runs 5` numbers, Status resolved); ticket 15's criterion 1 — "passes `check-line-before-stop` on 3 of 3 runs" — is struck through with the reason (that grader is deleted with the pre-action rule) and replaced by "`parallel-info-before-stop` and `stopped-report` pass 5 of 5", in the same format as ticket 03's dropped criterion. Ticket 03 itself: "Grader 改動" paragraph corrected to drop the `exec-finished` grader, Comments record the `ended`-only tightening of `stopped-line` (stricter than the spec's "either", deliberate: a `NOT confirmed` in an eval is a P1), and its Status becomes resolved after the outcome verifier's CONFIRMED. Ticket 12 gets Comments here — scope corrected to both session kinds, the stale criterion "grader asserting the kill command follows `TaskStop`" dispositioned as superseded by the spec's order (script first, then `TaskStop`), the offline criterion marked met — but its **Status stays open for ticket 04**, which owns the live proof. Ticket 04's item 4 and its acceptance line are reworded to "12 resolved by 04; 15 and 16 already resolved by 03, 04 appends the live evidence to their Comments" (done in this slice's commit).

## Non-goals

No change to the check interval, staleness threshold, `TaskOutput`/timer waiting, the Failures table, the cleanup rule beyond the one stop-order exception named in Scope, the merged three-heading presentation (`Consensus` / `Solo claims` / `Divergences`) when both runs succeed, or the READMEs/ADR (ticket 04). No general Codex process management. No new stub mode. The question-text landing (`AskUserQuestion` first line) is **written but untested**: the eval grants no `AskUserQuestion`, so no run reaches it (recorded in ticket 03's Comments as known-untested; ticket 04's interactive live stop is where it can be seen).

## Scope (files)

- `skills/ask/SKILL.md`: step 8 (command; stop path; fixed wording — the stop report is "the line `stop.sh` printed"; the parallel paragraph rewritten per outcome 3), step 10 item 1 (restate the stop line) and the **Parallel reply** paragraph (outcome 4), step 11: the rule "run cleanup before the message in which you tell the user about any stop" gains one explicit exception — for a stopped run, `stop.sh` runs while `<tmp>` still exists and the `Consultation stopped:` line plus `TaskStop` come first; cleanup follows immediately after `TaskStop` and still precedes the final reply (ticket 13's "every stop is a cleanup" is unchanged; the `pid` file is inside `<tmp>` and goes with it). **Any grader not named in this scope is left untouched** (`no-violations`, `sol-present`, `skill-fired` and the rest stay as they are).
- `evals/timeout-stalled-stop/graders/`: new `used-run-script.md` (`tool_used` Bash, `input_match` `scripts/run\.sh[^"]*codex[^"]* exec`, min 1) and `used-stop-script.md` (`tool_used` Bash, `input_match` `scripts/stop\.sh`, min 1); `stopped-line.md` rewritten (last message; full-field regex tolerant of `**`, backticks and `-` for `—`: `Consultation stopped:[*_` ]*interval \d+ minutes \((default|override)\); elapsed \d+:\d\d; (last event \S+ \d+:\d\d ago|no events); offered: wait another \d+ minutes / stop \(recommended: (wait|stop)\); process tree ended`); new `stopped-at-stop.md` (`target: trace`: `"role":"assistant"[^\n]*Consultation stopped:[^\n]*process tree ended[\s\S]*"name":"TaskStop"` — the model's own line, on an assistant line, earlier in the trace than the `TaskStop` call); `stop-report.md` (LLM) reworded to the fixed fields; keep `used-taskstop`, `one-codex-exec`, `temp-cleanup`, `no-bare-cd`, `skill-fired`.
- `evals/parallel-shared-timer/graders/`: delete `check-line-before-stop.md`; new `used-run-script.md` (min 2) and `used-stop-script.md`; new `parallel-info-before-stop.md` (`target: trace`: `"role":"assistant"[^\n]*done [—-] [*` ]*gpt-5\.6-sol[*` ]*; still running [—-] [*` ]*gpt-6-astra[\s\S]*"name":"TaskStop"`); new `used-stop-script.md`; new `stopped-report.md` (last message: the full-field stop line as above followed by `; done [—-] [*` ]*gpt-5\.6-sol[*` ]*; still running [—-] [*` ]*gpt-6-astra`); keep `check-line`, `stopped-line` (`Failed model: gpt-6-astra — stopped`), `used-taskstop`, `exec-twice`, `two-background-runs`, `two-temp-dirs`, `temp-cleanup`, `no-bare-cd`, the per-run argv graders, `timer-llm`.
- `evals/_harness/ticket-r03-graders.test.mjs` (new; precedent `ticket08-graders.test.mjs`): each new/rewritten regex passes the exact line `stop.sh` prints (shape from `run-stop-scripts.test.mjs`) with and without `**`/backticks, and fails: a line missing `offered:`, one missing `elapsed`, `process tree NOT confirmed` where `ended` is required, a paraphrase, **a synthetic two-line trace where the line sits only in a `"role":"user"` tool_result line before the `TaskStop` call** (must fail `stopped-at-stop` and `parallel-info-before-stop`), and a trace where the assistant line comes only after `TaskStop`. `codex-call-regex.test.mjs` gains the `run.sh … -- codex exec` shape as a positive sample and the `stop.sh` call as a negative one.
- Tickets: 03 Comments (rounds, runs, results, the dropped criterion, the untested landing); 12 Comments (scope correction, stale clause, offline criterion met; Status unchanged); 15 and 16 resolved; 04 items reworded per outcome 5; 08 Comments (reliability: "single green → 2 of 3 → 5 of 5 after slice 03").

## Dropped criterion (ticket 03, "每個 run 的 `.stub/exec-finished` 都不存在")

Dropped, and the ticket's criterion is rewritten to say so. `timeout-stalled-stop` seeds `slow-silent` with `duration_s 600` and the eval session ends long before 600 s, so the file is absent whether the kill worked or not — a file-absence grader would not discriminate, and the harness behaviour of a `source: file` grader on a missing file is unknown. The evidence that the script really ends and verifies the tree is ticket 02's offline test (`run-stop-scripts.test.mjs`: `exec-finished` absent after `stop.sh`, `events.jsonl` still) and ticket 04's live PID snapshots; this slice proves that the skill *drives* the script correctly.

## Eval plan (sonnet, `--allow-tools Bash Write TaskOutput TaskStop`)

| Step | Cases | Runs | Expect |
|---|---|---|---|
| Offline first | all `evals/_harness/*.test.mjs` (except the pre-existing CRLF failure of `ticket03-patterns`), `claude plugin validate` | — | green |
| Red (unchanged skill, new graders) | `timeout-stalled-stop`, `parallel-shared-timer` | 1 each | fail `used-stop-script`, `stopped-line`/`stopped-report` (`process tree ended` absent), `stopped-at-stop`; other graders as before |
| Reachability | `timeout-stalled-stop` on the new skill | 1, `--keep-temp` | 1.00, and the kept trace shows `bash … scripts/run.sh` and `scripts/stop.sh` executing inside the sandbox (no "not found"); every new regex is run against this real `trace.jsonl` with `node -e` before any further run, so the field-order assumption is checked on real bytes, not only on the synthetic sample |
| Green | `timeout-stalled-stop`, `parallel-shared-timer` | 4 more + 5, `--keep-temp` on the last of each | all 1.00 (5/5 each, counting the reachability run) |
| Regression | `manual-with-question`, `fail-nonzero-exit`, `followup-carries-claims`, `timeout-alive-notice` | 1 each | 1.00 |

Kept traces are read for the order stop.sh → line → `TaskStop` → cleanup → restated, and copied to `.scratch/ask-codex-reliability/evidence/03-<case>-trace.jsonl` before the kept directory is deleted.

## Acceptance

1. Offline checks green; `claude plugin validate` passes; the CODEX_CALL regex counts the prefixed command as exactly one `codex exec` and ignores the `stop.sh` call; the synthetic tool-result-only trace fails both trace graders.
2. `timeout-stalled-stop` 5/5 at 1.00 and `parallel-shared-timer` 5/5 at 1.00 on the final skill bytes, with no skill or grader edit after those runs started.
3. In every kept trace the order is `scripts/stop.sh` → assistant line with `Consultation stopped:` → `TaskStop` → `rm -rf -- '<tmp>'` → final message, so `temp-cleanup` and `stopped-at-stop` are green together; the `Consultation stopped:` line printed by `stop.sh` appears verbatim in an assistant line before the `TaskStop` call and again in the final message; in the parallel trace the `done — …; still running — …` fields appear on an assistant line before `TaskStop`, and the final message carries the stop line, the `Failed model:` line with the same elapsed, and one `Parallel check:` line per check.
4. Regression sample 4/4 at 1.00.
5. Fresh outcome verifier CONFIRMED on the exact claim: **"On the final bytes, the skill launches through `run.sh`, stops through `stop.sh` before `TaskStop`, writes the script's stop line at the stop and restates it in the final message, and in a parallel stop carries the done/still-running fields before `TaskStop` — 5 of 5 runs in each of the two stop cases, regression sample 4/4. That the script itself ends and verifies the tree is proven by ticket 02's offline test, not by these evals; the live guarantee is ticket 04's."** The verifier reads the kept traces and reruns the offline suites.
6. The slice lands as one commit (rollback: revert it); tickets 15/16 resolved, 12 commented, 04 reworded in the same commit.

## Stops

Any green run below 1.00 after two full-fix passes → pause and report (five passes is the hard budget). A `process tree NOT confirmed` in an eval run is a P1 (script or harness), not a wording issue. Any change to `run.sh`/`stop.sh` reopens ticket 02's offline test first. If the reachability run shows the sandbox cannot execute the scripts, stop and report before spending the remaining runs.

## Narrowing (user decision, 2026-09-18, after five fix/reverify passes)

Five passes exhausted the contract's budget without 5/5 in both cases. Findings that changed the design on the way (each offline-tested first): the eval sandbox gives every command its own PID namespace (cooperative stop through `run.sh` + `stop-request`/`stop-result`); `run.sh` lost the prompt because a background job's stdin defaults to /dev/null (fixed, regression-tested); the `TaskOutput` timeout wording let the model wait past the interval; a procedural hint in `stop.sh`'s stderr was followed over the skill's order (removed); the final-answer fixed lines now come from `<tmp>/stop-report`, shown by the stopped run's cleanup line right before the answer. The one residual failure is model-level: about one run in five to ten calls `TaskStop` in a message with no text at all, so the report line is not written *before* the call in that run (it is still written by the script, and still opens the final answer).

Narrowed acceptance, chosen by the user over another round: `stopped-at-stop` passes in **at least 4 of 5** runs of `timeout-stalled-stop`; every deterministic grader other than that passes 5/5 in both stop cases; the `timer-llm` judge was reworded so that the fixed line's `still running — <slug>` field (state at the check) is not read as "kept waiting"; the regression sample stays 4/4. The verifier claim in acceptance 5 is narrowed accordingly.
