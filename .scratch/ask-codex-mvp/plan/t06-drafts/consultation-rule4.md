(Replacement for the `followup_status` part of rule 4 in `skills/ask/prompts/consultation.md`.)

Current: "... and `followup_status` = `null`."
New:     "... and `followup_status` = `null` unless the Consultation type section above tells you to report follow-up statuses."
