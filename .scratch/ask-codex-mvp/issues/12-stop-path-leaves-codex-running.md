# 12 — Stopping a consultation leaves Codex running (headless sessions)

**What to build:** when a consultation is stopped — by the stall question, by the non-interactive stop rule, or by the user — the Codex process chain it started actually ends **in every kind of session**. Today the skill calls `TaskStop` and reports `Consultation stopped:`, and that is enough in an interactive session, but in a headless (`claude -p`) session on Windows the `codex.exe` it spawned keeps running: it continues reading files, keeps writing `events.jsonl`, and keeps spending API quota, while the user has been told the consultation is over.

**Scope (measured, ticket 09):** the leak is specific to the headless path. In the interactive scenario I3 the user chose "stop" at the stall question and the snapshot taken inside the still-open session listed exactly the six baseline PIDs and nothing else — the chain ended. In the headless A8 and in the direct A8b probe, driven by `TaskStop` on a background task, the chain survived. So the fix targets how a background-task stop propagates, not the stop logic itself.

**Blocked by:** None — can start immediately (found during ticket 09 live acceptance; ticket 09 records F8 as failing because of it).

**Status:** resolved (reliability tickets 02, 03 and 04, 2026-09-20)

## Evidence (ticket 09, 2026-09-16)

- **A8** (the skill's own stop path, headless, `EVAL_ASK_CODEX_TIMEOUT_MINUTES=1`): the skill detected the stall correctly, called `TaskStop`, and reported the stop. Its own snapshot ~5 s later showed three `codex` processes alive that were absent from the pre-run baseline (PIDs 49108, 31216, 13432, `@openai+codex@0.154.0`). They were still alive minutes later.
- **A8b** (direct probe, same stop mechanism): `TaskStop` returned `Successfully stopped task`, yet the whole tree survived at +5 s and +75 s — `sh.exe → node.exe` (the pnpm shim) `→ codex.exe`, the last carrying exactly the argv that was sent. `events.jsonl` grew from 788 to 2579 bytes *after* the stop, so the consultation was still working; `last-message.json` never appeared. The chain only ended when killed with `Stop-Process -Force`.
- Cause: `TaskStop` ends the harness's task (and its shell), but the `codex.exe` grandchild launched through the pnpm shim is not part of what gets killed.

## Acceptance criteria

- [x] After a stop, no `codex` process that was absent from a baseline taken before the run remains — verified by a snapshot taken inside the still-running session, a few seconds after the stop.
- [x] The stop path kills the process tree, not just the task: on Windows via the equivalent of `taskkill /T /F /PID <pid>` for the shell it started, elsewhere via a process-group kill; the skill records the pid when it launches the run so it has something to kill.
- [x] The stop report is only written after the kill has been verified; if a process survives, the report says so instead of claiming the consultation was stopped.
- [x] `events.jsonl` stops growing after the stop (the check that proves the run is not merely orphaned but genuinely ended).
- [x] ~~An eval covers it offline: the stub's `slow-silent` mode plus a grader asserting the kill command follows `TaskStop` in the trace, since the process check itself cannot run in the eval sandbox.~~ Superseded (reliability slice 03): the spec fixes the opposite order — the stop script runs *before* `TaskStop` — and the offline graders are `used-run-script`, `used-stop-script`, `stopped-at-stop` (the script's report line, ending `process tree ended`, written by the assistant before the `TaskStop` call) and `stopped-line` (restated in the final reply).
- [x] The README's timeout section states the guarantee that actually holds after the fix.

## Comments

**2026-09-18 — scope corrected; offline part done by reliability slice 03; Status stays open until ticket 04's live proof.** The scope line "specific to the headless path" was wrong: reliability probe A (interactive session, `TaskStop` on the background task) showed the same `sh → node → codex.exe` chain running to completion and spending the whole consultation's quota, exactly as the headless probe B did (`.scratch/ask-codex-reliability/evidence/01-stop-behaviour.md`). The residue is a property of `TaskStop` on a background Bash task on this platform, in both session kinds. Fix, unconditional: the skill launches through `skills/ask/scripts/run.sh` (records the child's pid, watches for a stop request) and stops through `skills/ask/scripts/stop.sh` (kills the tree directly when the pid is visible, and through `run.sh` from inside the child's process namespace when it is not — the eval sandbox gives every command its own PID namespace, which the first reachability run exposed as `pid 7 is not running`), verifies nothing survives and that `events.jsonl` stays still for 5 s, and prints the one `Consultation stopped:` line the model copies; only then `TaskStop`, then cleanup. Offline proof: `run-stop-scripts.test.mjs` (31/31 on Git Bash and WSL) and slice 03's eval runs (recorded in ticket 03). The live PID-snapshot criteria of this ticket are ticket 04's; the README criterion is also ticket 04's.

**2026-09-18 — slice 03 numbers.** On the final bytes, three rounds of five: `used-run-script`, `used-stop-script`, `process tree ended` in the report 15/15 in both stop cases — the skill drives the scripts every time; the scripts' own kill and verification are proven offline (`run-stop-scripts.test.mjs` 34/34 on Git Bash and WSL). The live PID-snapshot proof remains ticket 04's; Status unchanged.

**2026-09-20 — resolved: live proof on real Codex in both session kinds (reliability ticket 04).** Scope corrected first: this ticket said the leak was specific to headless sessions; reliability ticket 01 showed `TaskStop` ends nothing in an interactive session either, so the fix is unconditional and both kinds were verified. Evidence: `.scratch/ask-codex-reliability/evidence/04-live-stop.md` with the recordings `04-live-{H,I}-watch.json` and the replies `04-live-{H,I}-reply.md`; re-checkable with `plan/live-04-check.mjs` (exit 0 for both, from the evidence files alone). Headless: the whole chain `sh → node → codex.exe` plus Codex's own children (code-mode host, sandbox setup, command runner, powershell, conhost) was gone 1.4 s after the stop request, before `stop.sh` wrote its report; nothing of it alive at +5 s or +60 s; no `codex.exe` outside the baseline at +60 s; `events.jsonl` constant for 10.8 s (43 samples) until the skill deleted the run directory; `last-message.json` never appeared. Interactive (the user chose "stop" at the stall question while Codex was inside a `Start-Sleep 240`): gone after 1.2 s, the sleeping `powershell.exe` included; 12.7 s / 50 samples constant. Both replies carry the script's `Consultation stopped: … process tree ended` line before `TaskStop` and as the first line of the final answer, and attribute nothing to Codex. Guarded files unchanged by the runs. Eval side (ticket 03): `used-run-script`, `used-stop-script` and `process tree ended` 15/15 in both stop cases; scripts' offline test 34/34 on Git Bash and WSL. Decision recorded in ADR 0004. Known residual, stated in both READMEs: about one stop in five to ten the line appears only at the top of the final reply.
