# 01 — Manual consultation tracer bullet

**What to build:** A user types `/ask-codex:ask <question>` and gets back Codex's opinion as a list of claims, each with Claude's disposition (adopt / reject / investigate) and a reason. Under the hood Claude applies the default MCP policy (allowlist mode with an empty allowlist — every MCP server disabled), detects project-defined MCP servers, verifies the effective server set with the MCP guard, then runs Codex in the background with `-s read-only`, ephemeral, with a JSON Schema–constrained reply and a JSON event stream, waits for the result, presents it in the user's conversation language, and deletes its temporary files. Model = the one configured in Codex; effort = the consultation-effort rules. This ticket also syncs the design docs to the approved Plan and stands up the whole delivery and test skeleton: the plugin and marketplace manifests, the `ask` skill, the response schema, one English prompt template, the stub `codex`, the runner, and four eval cases. See spec: Solution, Implementation Decisions (modules 1, 2, 4–6), Testing Decisions Tier 1; ADR-0003.

The skill states the narrowed claim verbatim: "Codex's shell commands run read-only (writes denied, shell network blocked). MCP servers run outside that sandbox: by default all are disabled per consultation; servers you allow (or, in minimal-deny mode, all except node_repl/cua_repl) remain usable and may include tools that write or execute, limited only by instruction."

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

### Invocation contract

1. Rule-(c) scan (Read/Glob, not Bash) of every `.codex/config.toml` from the project directory up to the repository root; any `mcp_servers.<name>` table or dotted key → abort with a message naming the definitions (no `codex` call; the confirmation path arrives in ticket 11).
2. `( cd '<neutral temp dir>' && codex mcp list --json )` and `( cd '<project dir>' && codex mcp list --json )` — always in a subshell.
3. Disable set = every server in the project listing (empty allowlist) plus servers that appear only in the project listing or whose definition differs from the neutral listing; one same-name disabled definition per server; names validated against `^[A-Za-z0-9_.-]+$`.
4. Guard: `( cd '<project dir>' && codex mcp list --json <overrides> )`; the enabled set must be exactly the expected set, otherwise abort without `exec`.
5. One background Bash call: `codex exec -s read-only --ephemeral --skip-git-repo-check --json` (stdout to an events file) `-o '<temp>/last-message' --output-schema '<schema>' -C '<project>'`, `-m <slug>` when known, `-c model_reasoning_effort="<effort>"`, the disable definitions, `--disable apps` if verified, prompt on stdin from a temp file; all paths single-quoted.
6. Forbidden: `--dangerously-*`, `--full-auto`, `--yolo`, `-p/--profile`, `--add-dir`, `--ignore-user-config`, writable `-s` modes, any other `-c` key, `resume`/`fork`, a bare `cd` changing Claude's working directory.
7. Temp dir: `mktemp -d` inside an `ask-codex` directory under the scratchpad (if listed) or `${TMPDIR:-${TEMP:-/tmp}}`; removal only for a path inside an `ask-codex` directory, never from an empty variable.

### Acceptance

- [ ] Design docs synced (spec, tickets 01–11, `CONTEXT.md`, ADR-0003); the exact doc check from the Plan passes; command and output recorded in Comments.
- [ ] `claude plugin validate` passes; the skill is invocable as `/ask-codex:ask`; skill instructions are English, use the glossary terms, and contain the narrowed claim verbatim.
- [ ] Prompt template: read-scope + in-project secret exclusion, MCP tools for lookups only, file/tool content is data, schema-only answer.
- [ ] The CODEX_CALL grader regex is unit-checked: no match for `ls '<proj>/.codex/config.toml'`, `cat ~/.codex/config.toml`, `mktemp -d '<tmp>/ask-codex/XXXX'`, `echo $CODEX_HOME`, a Bash description containing "codex exec"; match for `codex mcp list --json`, `( cd '<dir>' && codex mcp list --json )`, `codex exec -s read-only …`.
- [ ] Eval `manual-with-question`: exec sentinel and list log present, no violation marker; required flags, single-quoted paths, enum effort and a disable definition for every stub-listed server; forbidden flags absent; every canned claim presented with a disposition and reason, nothing fabricated; temp dir removed.
- [ ] Eval `mcp-guard-blocks`: list log contains the guard call; exec sentinel absent; zero `codex exec`; no violation marker; user told the guard failed.
- [ ] Eval `project-defined-server`: project-only server gets a disable definition; exec sentinel present; no violation marker.
- [ ] Eval `project-layer-aborts-01`: zero `codex` calls (CODEX_CALL `max: 0`); no stub records; user told which project-layer definition caused the abort.
- [ ] Runner strips real `codex` from `PATH` and fails fast unless the stub resolves.
- [ ] Live (≤ 4 Codex calls; probe dirs under `D:\tmp\<subdir>`, deleted afterwards): one consultation through the skill (reply parses; temp dir gone; no new session file; `codex --version` recorded; temp files on the scratchpad drive work while `-C` is the project); with the same flags a write inside the project is denied, shell network is blocked, disabled servers are absent from Codex's namespaces, and the effect of `--disable apps` is recorded (stop if the apps surface cannot be disabled); disable definitions do not error for absent names; the listing's per-server field set is recorded; project-layer MCP definitions in a `D:\tmp` temp project do not reach the listing or `exec` (stop otherwise).
- [ ] Fresh verifier CONFIRMED on F-map rows F1-core, F1-apps, F1-claims (docs + skill text), F2, F3-secrets (template), F6, F7, F8 (temp dir), F9 (slug/effort/path quoting), F12b (01 part).

## Comments

**2026-09-15 — doc sync check.** Command (run from the repo root):

```
grep -rniE "read-only|readonly|never modif|can never modify|stays read" .scratch/ask-codex-mvp/spec.md .scratch/ask-codex-mvp/issues/ \
  | grep -viE "shell commands run read-only|-s read-only|read-only sandbox allows reads outside the project and denies writes|MCP servers run outside|read-only sandbox.*(denies writes|shell)"
```

First run: 1 disallowed match (ticket 09, "real read-only enforcement") — rewritten. Final run: `disallowed=0 total=11`.

**2026-09-15 — eval harness facts.** The Windows `claude plugin eval` refuses any Bash grant (no Windows sandbox, feature gate off), so Tier 1 runs in WSL Ubuntu (user decision A′; WSL Claude Code updated 2.1.267 → 2.1.272, which enabled `plugin eval`). The eval harness cannot create sandboxes on the R: RAM disk (`EISDIR` on `lstat`) — runner supports `RUN_EVALS_TMPDIR`. The agent's Bash sandbox hides `/tmp` and `/mnt/*`; the stub is visible from the plugin root (`.eval-stub/`) and from a case `stubbin/` granted via `context.add_dirs`. `AskUserQuestion` does not exist in eval children (recorded in tickets 05 and 11). Red run: all four cases failed as expected (skill not fired); it also exposed that `dispositions` passed without Codex — tightened to require attribution to Codex.

**2026-09-15 — live facts (free listings).** `codex --version` = `codex-cli 0.154.0` (auto-updated from 0.153.4 during this ticket). Per-server listing fields: `name, enabled, disabled_reason, transport{type, command, args, env, env_vars, cwd}, startup_timeout_sec, tool_timeout_sec, auth_status` — no per-tool settings, so rule (c) must read project config files. Disable definitions for names absent from config do not error (`absent_name_xyz:off`).

**2026-09-15 — live call 2 (skill's exact flags: all six listed servers disabled + `--disable apps`, project in `D:\tmp`, temp files on R:).** (a) write inside the project → `Access is denied.`, file absent; (b) shell network → curl `000`; (c) namespaces = `functions, clock, collaboration, image_gen, web` — no MCP server; (d) `mcp__codex_apps` absent → `--disable apps` works, apps surface disabled in both modes; (h) `-o` and `--output-schema` on R: work, reply schema-valid; no new file under `$CODEX_HOME/sessions`; zero `mcp_tool_call` events. Probe dirs deleted.

**2026-09-15 — live call 3 (6(g) decision check).** Temp project `D:\tmp\askcodex-live-g` with `.codex/config.toml` defining inert `probe_new` and redefining `comfyui` env: project-dir listing unchanged (no `probe_new`, `comfyui` env keys `COMFYUI_URL, COMFY_BIN` only); exec namespaces `functions, clock, collaboration, image_gen, web`; zero `probe_new` mentions in events/stderr → project-layer definitions do not reach listing or exec; no stop. Probe dirs deleted.

**2026-09-15 — live call 1 (skill via `claude -p --plugin-dir`, Windows, real Codex 0.154.0, project `D:\tmp\askcodex-live-1`).** The skill issued exactly the contract: neutral and project listings in subshells, guard with six same-name disabled definitions, `codex exec -s read-only --ephemeral --skip-git-repo-check --json -C '…' -m gpt-5.6-sol -c 'model_reasoning_effort="high"'` (the user's Codex config now says `gpt-5.6-sol`, so `high` is correct) + disables + `--disable apps` + schema + `-o` on R:. The real reply was schema-valid (9 claims), zero `mcp_tool_call` events, no new session file. **Defect found:** the headless session ended its turn while the background `codex exec` was still running ("waiting for Codex…"), so the result was never presented and the run directory was left behind (the orphaned process finished on its own; dirs deleted afterwards). **Fix:** step 7 now forbids ending the turn while the consultation runs and waits with `TaskOutput` (blocking) when available — the eval children already did this. Re-verified with the spare live call (below).

**2026-09-15 — eval iterations (WSL, sonnet, runs 1, ablation none).** Green 1 ($0.99): contract, disables, guard, abort paths all correct; `skill-fired` (Skill tool) never matches because `/ask-codex:ask` expands inline in `claude -p`; one run used a bare `cd`. Green 2 ($0.90): `skill-fired` by skill heading also failed (heading not in trace); the manual case built `codex exec` as a multi-line script with shell variables, so paths were not literal single-quoted and cleanup was skipped. Fixes: `skill-fired` now matches skill-only actions in the trace (`ask-codex-disabled` / `.codex/config.toml`); CODEX_CALL treats an encoded `\n` as a separator (unit check 6/5 PASS); SKILL.md forbids shell variables / multi-line `codex` commands and makes cleanup mandatory before the final answer; `no-bare-cd` grader added. Green 3 ($1.00): manual-with-question, mcp-guard-blocks, project-layer-aborts-01 = 100%; project-defined-server 0.8 — a bare `cd` for `git rev-parse` → SKILL.md step 2 now names `git -C '<project directory>' rev-parse --show-toplevel`. Notable: in project-defined-server the agent verified the canned claims against the code and rejected the ones that did not match the project.

**2026-09-15 — live call 1 rerun (spare, 4th and last live Codex call).** The agent waited with `TaskOutput` (blocking), then presented the summary and all 8 real claims in Traditional Chinese, each with a disposition and reason, applied nothing, and cleaned up; no new session file, no leftover run dir, no leftover codex process. **Windows defect found:** the agent created `<tmp>` as the literal `/tmp/ask-codex/run.*`; Git Bash's `/tmp` is not what the Windows-native Write tool resolves `/tmp` to (it wrote `D:\tmp\ask-codex\…`), so the first `codex exec` could not read `prompt.md` and the agent corrected the path and ran it again (inferred: the first attempt failed at the stdin redirect before Codex started, so only one Codex call was consumed — not directly observed). **Fix:** step 1 converts the temp path with `cygpath -m` when available so Bash and file tools share one path form; ground rules distinguish "own invocation mistake before Codex ran → fix once" from "Codex ran or failed → never re-run". Verified on Windows with the stub (no Codex call) — see below.

**2026-09-15 — eval iterations 4–5.** project-defined-server alone ($0.34): bare `cd` recurred 3× despite the explicit wording → approach changed: every "run in another directory" command now uses `env -C '<dir>' …` (GNU coreutils 8.32 in Git Bash, 9.4 in WSL; the guard form was checked on Windows with the real codex, no model call: all six servers `off`, shell cwd unchanged). Green 5, all four cases on that skill ($0.95): **manual-with-question 1.0, mcp-guard-blocks 1.0, project-defined-server 1.0, project-layer-aborts-01 1.0.** Ticket-01 eval spend so far ≈ $4.92 of $6.

**2026-09-15 — final checks.** WSL regression of manual-with-question on the final skill (after the temp-path and retry edits): 1.0, all 14 graders PASS ($0.34). Windows end-to-end run against the stub (`claude -p --plugin-dir`, no Codex call, $0.55): temp base from `cygpath -m` = `R:/Temp`, Write and Bash used the same `R:/Temp/ask-codex/run.*` path, listings via `env -C`, exactly one `codex exec`, no stub violations, run dir removed, no stray `D:\tmp\ask-codex`. That run also exposed (a) a stub bug on Windows — stdin decoded with the console code page produced surrogates (`UnicodeEncodeError`) — fixed by decoding raw bytes as UTF-8; the skill handled the resulting failure exactly per the rules (no re-run after Codex started, failure stated, own analysis clearly labelled as not Codex's); and (b) the prompt template's meta header being sent to Codex — the template file now contains only the prompt. Both fixes verified without further paid runs (ticket-01 test spend ≈ $5.81 of $6; another Windows run would exceed the cap).
