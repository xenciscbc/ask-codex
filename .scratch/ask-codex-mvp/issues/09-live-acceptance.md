# 09 — Live acceptance

**What to build:** The tier-2, opt-in verification: in a real Claude Code session with the real Codex CLI, walk through every acceptance scenario and record the outcome, the Codex CLI version, and any behaviour that differs from the spec. This confirms what the offline evals cannot — real enforcement of the shell sandbox (writes denied, network blocked) and of the MCP policy, real schema-constrained replies, real consent prompts, and real timing. Prior art: `codex-feather` keeps live runs behind an explicit opt-in. See spec: Testing Decisions, Tier 2.

**Blocked by:** 02 — Consultation types and packaging; 03 — Model aliases and effort rules; 04 — Failure handling; 05 — Proactive consultation and consent; 06 — Follow-up consultation; 07 — Timeout monitoring; 08 — Parallel consultation; 11 — MCP policy configuration and setup.

**Status:** ready-for-agent

Live probe directories live under `D:\tmp\<subdir>` and are deleted afterwards. Budget: ≤ 16 Codex calls.

- [ ] Manual consultation with a question: shell writes denied and shell network blocked with the exact flags (F2), disabled MCP servers absent from Codex's namespaces, structured claims returned, dispositions presented.
- [ ] Manual consultation without a question: question inferred from context.
- [ ] Parallel `astra` + `sol`: efforts `medium` / `high`, grouped presentation.
- [ ] Simulated fix loop: consent prompt appears; all three options behave as specified.
- [ ] Ambiguous alias `5.6`: candidates offered.
- [ ] Wrong model name: no retry, no fabricated opinion.
- [ ] Follow-up consultation: only carried claims are verified; no resume.
- [ ] Timeout with a short override: auto-wait while active, prompt when stalled.
- [ ] A project path containing spaces works (F9).
- [ ] After "stop", no Codex process remains (F8).
- [ ] MCP policy: default (all disabled), an allowed lookup server usable, minimal-deny mode, and a project-layer definition prompt with decline → abort.
- [ ] Results, the Codex CLI version (`codex --version`, F10), and any deviations (plus any cases moved from tier 1 to tier 2) are recorded in the ticket comments for the README.

## Comments

**2026-09-15 — moved from ticket 03.** The interactive override-scope question (`AskUserQuestion`: "this consultation only" vs "rest of the session") cannot run in evals (no `AskUserQuestion` in eval children). Live acceptance here must include one consultation that names a model different from the session setting and shows that question, and one later consultation that confirms a session-scoped choice persists.

**2026-09-15 — moved from ticket 07.** Live acceptance here must cover what evals could not: (1) the interactive stall question via `AskUserQuestion` (options "wait another T minutes" / "stop this consultation", elapsed time, last event and age, recommendation by the T/2 rule), including choosing "wait" at least once; (2) F8 — after "stop", no Codex process remains (the whole process chain ends); (3) the stop report's wording, which passed an eval judge only 2–1 (and offered to "retry" as a new consultation). Also watch `temp-cleanup` (one skipped cleanup was seen in ticket-07 evals and not reproduced after the fix).

**2026-09-16 — execution mode (user decision).** The automated scenarios run headless by Claude (real Codex CLI, ≤ 16 calls in total, probe projects under `D:\tmp\<subdir>`, deleted afterwards). The three scenarios that need a person answering `AskUserQuestion` — the proactive consent prompt's three options, the override-scope question (this consultation / rest of session), and the stall question answered with "wait" at least once — are written up as step-by-step instructions for the user to perform in an interactive session; Claude then reads that session's transcript and records the outcome. Ticket 05 is paused, so the fix-loop scenario's outcome is recorded as observed (it may not propose reliably).

**2026-09-16 — slice-09 readiness: READY on revision 3.** Round 1 REVISE (5 blockers) → revision 2: the guarded files are named (`C:\Users\admin\.claude\ask-codex.json`, `D:\codex\config.toml`), backed up to `D:\tmp\askcodex-live-09-backup\` and hash-checked before and after every run with restore-and-stop, plus a Rollback section; slice-local stops with the sandbox/namespace probe A2 running first; A2 records Codex's own tool namespaces and fails if any of the six servers or `mcp__codex_apps` appears; the run prompts print each `events.jsonl` `mcp_tool_call` line and each `last-message.json` before cleanup, checked against the schema by `live-09-check.mjs`; an F8 process baseline via `Get-CimInstance Win32_Process`, a forced-stop probe A8b, and a narrowing fallback if no scenario reaches a stop. Round 2 REVISE (2 blockers), both FIX → revision 3: the F8 post-stop snapshot must be taken **inside the stopping session while it is still running** (headless: 5 s after `TaskStop`, before the final reply; I3: by the user via `!` before quitting), because a snapshot after `claude -p` exits could be cleaned up by the session's own exit; the interactive guide drops `tasklist` for a baseline handoff plus the in-session snapshot; A8b must use A2's exact command line (`-s read-only`, `--ephemeral`, `--disable apps`, one disable definition per listed server); stop 3 now names every default-policy scenario (A1, A2, A4, A7, A8, A8b, A9). Closing review: READY. Budget: ≈ 14 Codex calls of the 16 allowed (5 of 25 used program-wide). Entry gate: ticket 08's outcome verifier CONFIRMED.

**2026-09-16 — entry gate met, and the first guarded-file event (re-baselined by user decision).** Ticket 08 is resolved (verifier CONFIRMED on eb87a93, resolution fc24488). Preparation: the throwaway project `D:\tmp\askcodex-live-09\interactive` (fetchUser sample: `src/user.js` swallowing `TimeoutError` into `{}`, `src/api.js`, `src/pages/profile.js`, `package.json`), the A2 probe project `D:\tmp\askcodex-live-09\a2`, and the guarded-file baseline at 07:54 — `C:\Users\admin\.claude\ask-codex.json` absent (as expected), `D:\codex\config.toml` SHA-256 `a6efa412…`, both backed up to `D:\tmp\askcodex-live-09-backup\`.
At 08:09, before the first scenario, `config.toml` changed to `eacf4218…` with no Codex call of ours in between. The diff is confined to the Codex app's own runtime bookkeeping: the `cua_node` runtime directory hash (`a708e72b10c27b59` → `6f12e0ef1c6e5061`) in `node_repl`'s command, module dirs, node path and trusted paths, `BROWSER_USE_CODEX_APP_VERSION` (`26.908.40834` → `26.908.70816`) with the matching browser-service path, the `SKY_CUA_NATIVE_PIPE_DIRECTORY` GUID and `CODEX_CLI_PATH`. Same size (8043 bytes); `model`, `model_reasoning_effort`, and the membership and `enabled` state of every `mcp_servers` entry are unchanged. Restoring the backup would have pointed `node_repl` at a deleted runtime directory, so the literal contract action was harmful here. Asked, the user chose **re-baseline and continue**: the backup and hash were refreshed to `eacf4218…`, and slice 09's Safety section now distinguishes an app self-rewrite of those runtime keys (diff, record, re-baseline, continue) from any change to `model`, `model_reasoning_effort`, `approval_policy`, `sandbox_mode` or the `mcp_servers` membership/`enabled` state (still a stop).
Live facts recorded for A12/F10: `codex-cli 0.154.0`; the live `codex mcp list --json` shows six servers — blender, comfyui, cua_repl, node_repl, pencil enabled and codex_app disabled; the config's defaults are `model = "gpt-5.6-sol"`, `model_reasoning_effort = "medium"`.

**2026-09-16 — A2 (sandbox and namespaces) PASSED; two Codex calls.** Run directly with A1's flag set, not through the skill: `codex exec -s read-only --ephemeral --skip-git-repo-check --json -C D:/tmp/askcodex-live-09/a2 -m gpt-5.6-sol -c model_reasoning_effort="medium" -c mcp_servers.<name>={command="ask-codex-disabled",enabled=false}` for all six live servers, `--disable apps --output-schema … -o …`.
- **Write denied (F2):** Codex ran `powershell -Command 'echo probe > probe.txt'` and got `out-file : 拒絕存取路徑 'D:\tmp\askcodex-live-09\a2\probe.txt'。` (`FileOpenFailure`), exit 1; `probe.txt` does not exist anywhere under `D:\tmp\askcodex-live-09`.
- **Namespaces (F2, stop 2):** Codex reported its complete tool namespace list as `functions` and `collaboration` — none of blender, codex_app, comfyui, cua_repl, node_repl, pencil, and no `mcp__codex_apps`. `events.jsonl` contains zero `mcp_tool_call` lines.
- **Network:** the first attempt proved nothing — `curl -sS` is a PowerShell alias for `Invoke-WebRequest`, which rejected `-sS` before making any request. Reprobed with PowerShell-correct commands (second call, same flags, `a2brun`): `curl.exe -sS --max-time 10 https://example.com` → `curl: (7) Failed to connect to example.com port 443 after 16 ms: Couldn't connect to server`; `Invoke-WebRequest -Uri https://example.com -UseBasicParsing -TimeoutSec 10` → `無法連接至遠端伺服器`. Both exit 1, so egress is blocked; zero `mcp_tool_call` lines in that run too.
- **Guarded files:** unchanged before and after both runs (`config.toml` `eacf4218…`, `ask-codex.json` absent).
- **Environment note for the README:** each run emits one benign `error` item — "Skill descriptions were shortened to fit the skills context budget. Codex can still see every skill, but some descriptions are shorter." It is unrelated to the sandbox, but it means a consultation's `events.jsonl` can contain an `error` item on a fully successful run.
- The schema checker required by the contract is `.scratch/ask-codex-mvp/plan/live-09-check.mjs`; it validates the three required top-level keys, each claim's six required keys, the `kind`/`confidence`/`followup_status` enums and the array element types, and it was exercised on both A2 replies.

**2026-09-16 — A1 (manual consultation, default policy) PASSED; one Codex call (3 of 16).** Headless `claude -p` in `D:\tmp\askcodex-live-09\a1` with the plugin dir, `--permission-mode acceptEdits`, `--allowedTools Bash Read Glob Grep Skill Write TaskOutput TaskStop`, stream-json transcript kept outside the repo.
- **Command line:** `codex exec -s read-only --ephemeral --skip-git-repo-check --json -C 'D:/tmp/askcodex-live-09/a1' -m gpt-5.6-sol -c 'model_reasoning_effort="high"' -c 'mcp_servers.{blender,comfyui,cua_repl,node_repl,pencil}={command="ask-codex-disabled",enabled=false}' --disable apps --output-schema '…/consultation.schema.json' -o '…/last-message.json' - < prompt.md > events.jsonl 2> stderr.log`. Model and effort came from the config default plus the skill's effort rule (no model token in the request), which the reply states.
- **MCP guard:** the skill listed servers in a neutral directory and in the project (identical, no project-defined server), then relisted with the disable set: `blender=false, codex_app=false, comfyui=false, cua_repl=false, node_repl=false, pencil=false` — all six disabled, including `codex_app`, which needs no `-c` because the user config already disables it. The reply carries `MCP: all servers disabled for this consultation.`
- **Evidence printed before cleanup:** the run reported that `events.jsonl` contains no `mcp_tool_call` line and printed the full `last-message.json`; extracted from the transcript to `a1run/last-message-from-transcript.json`, it passes `live-09-check.mjs` (6 claims, 0 open questions).
- **Presentation:** every claim carries a disposition — C1–C5 adopted (C2, C3, C4 verified against `src/user.js` line by line), C6 marked investigate because the skill had not read `src/pages/profile.js` itself. Nothing was changed in the project (its four files are untouched).
- **Cleanup and guarded files:** the run directory was removed with `rm -rf -- 'R:/Temp/ask-codex/run.lkoEGR'`, no `ask-codex` directory remains under the Windows temp base, and both guarded files are unchanged.
- **Environment note (F9-adjacent, for the README):** the skill resolved the temp base to `R:\Temp\ask-codex` — the RAM disk. The consultation still succeeded because only the run directory (prompt, events, output) lives there while `-C` points at the project on `D:`; the known "codex exec fails on R:" problem applies to the *workspace*, not the run directory.

**2026-09-16 — A3 (consultation without a question) PASSED on the second attempt; one Codex call (4 of 16).** The first attempt was void through my own test design, not a skill defect: the context turn ended with "Do not consult any external tool or run codex", so the follow-up `/ask-codex:ask` was a direct instruction conflict. The skill stopped, named both instructions, asked which one holds, and ran **no** `codex` command — correct behaviour, but it tests nothing about question inference, so the turn was discarded (zero Codex calls spent). Redone with a context turn that only describes the symptom and asks for the two files to be read (the context turn has no `Skill` tool, so the prohibition was never needed).
- **Inference (the acceptance point):** the resumed turn, given only `/ask-codex:ask`, opened with one line naming the question it inferred — `Asking Codex：為什麼 API 變慢時，profile 頁面會顯示 "user not found"？（採 blind diagnosis，不會透露我目前的假設）` — which matches the symptom set in the context turn, and it kept the diagnosis blind.
- **Command line:** one `codex exec` with the full contract (`-s read-only --ephemeral --skip-git-repo-check --json`, `-m gpt-5.6-sol -c model_reasoning_effort="high"`, five disable definitions, `--disable apps`, schema and `-o`). Effort `high` is per the skill's own rule (item 6: no request → `gpt-5.6-sol` → `high`), not from the cache, whose `default_reasoning_level` for that model is `low` — below the skill's `medium` floor.
- **Timer:** stated the default, `No override — using the default 30 minute interval (staleness threshold 5 minutes)`.
- **Cleanup and guarded files:** `rm -rf -- 'R:/Temp/ask-codex/run.fthDUP'`; both guarded files unchanged before and after.
- **Observation for the `/code-review` pass (presentation, not a stop):** one claim was dispositioned `採納，但未驗證` ("adopt, but unverified") rather than one of the three fixed words, because Codex had read `src/api.js` and the skill had not. The reasoning is sound and stated, but the disposition vocabulary is not exactly `adopt` / `reject` / `investigate`.
- **Also observed:** this run passed `-C 'D:\tmp\askcodex-live-09\a3'` with backslashes (A1 used forward slashes); both work.

**2026-09-16 — A4 (parallel `astra, sol`) PASSED; two Codex calls (6 of 16).** Request: `/ask-codex:ask astra, sol Why does fetchUser …` plus the evidence-printing instruction.
- **Two runs, one contract each:** `-m gpt-6-astra -c model_reasoning_effort="medium"` and `-m gpt-5.6-sol -c model_reasoning_effort="high"`, each with `-s read-only --ephemeral --skip-git-repo-check --json`, its own run directory (`run.avytDr`, `run.gjZ0qh`), its own `-o`/`prompt.md`/`events.jsonl`, the full disable set (this run also emitted a `-c` for the already-disabled `codex_app`), `--disable apps` and the schema. The efforts match the skill's rule, and the reply says so — including that sol's `high` is the rule's fixed value, not the cache's `low`.
- **Check line and merge:** `Parallel check: done — gpt-6-astra, gpt-5.6-sol; still running — none.` on its own line, then all three headings in order with `none` under `Solo claims` and `Divergences` — the step-10 fix from ticket 08 working live. Claims are tagged `[both]` with full slugs and each carries `adopt`.
- **Evidence:** both `events.jsonl` reported free of `mcp_tool_call`; both `last-message.json` printed before cleanup (via the Read tool, so the transcript shows them line-numbered) and both pass `live-09-check.mjs` — 4 claims each, saved as `a4run/lm-1.json`, `lm-2.json`.
- **Cleanup and guarded files:** both run directories removed (`rm -rf -- 'R:/Temp/ask-codex/run.avytDr'`, `…/run.gjZ0qh'`); guarded files unchanged before and after.
- **Narrowing:** the two models agreed on everything, so `Solo claims` and `Divergences` were both empty. The live run therefore does *not* exercise the `Adopted: <slug> — <reason>` path for a divergence; that stays covered by the eval (`parallel-two-models`) only.
- **Minor observations:** the temp base `R:\Temp\ask-codex` remains after cleanup as an empty directory (0 bytes, no run directories) — the skill deletes run directories, not the base. One interim message during the run was written in Japanese although the session language is Traditional Chinese; the final reply was Chinese.

**2026-09-16 — A5 (ambiguous alias) and A6 (unknown model) PASSED; zero Codex calls, so the count stays at 6 of 16.** Both ran headless in their own projects with the full tool set, and the transcripts contain **no `codex` command of any kind** (not even `codex mcp list`), which is stop 5's condition.
- **A5 (`/ask-codex:ask 5.6 …`):** the alias matched three slugs from the live model cache — `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna` — and the run listed exactly those three, asked which to use in plain text (no `AskUserQuestion` in a headless session) and stopped, saying it would continue with the MCP checks only after the answer. The candidates come from the real cache, not from a list of its own: `gpt-5.6-sol` and `gpt-5.6-terra`/`luna` are the only `5.6` entries with `visibility: list`.
- **A6 (`/ask-codex:ask model nova …`):** no slug matched, so the run listed the five listed models (`gpt-6-astra`, `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`), said explicitly that the consultation stopped before sending any `codex` command, and attributed nothing to Codex. The hidden cache entries (`gpt-reserve`, `codex-auto-review`) were correctly not offered.
- **Guarded files:** unchanged before and after both runs.

**2026-09-16 — F8 baseline validated as necessary (no Codex call).** A snapshot taken between scenarios (`Get-CimInstance Win32_Process -Filter "Name like 'codex%'"`) lists **five `codex*` processes that none of these scenarios started**: PID 2524 (since 2026-09-14) and PIDs 37128 / 40976 (since 2026-09-15 11:01), all from the pnpm-installed `@openai/codex@0.153.4`, plus PIDs 34832 and 42444 started at 08:08:59 and 08:09:04 by the Codex app — the same moment it rewrote `config.toml`. A bare "is any `codex.exe` running?" check, which the first version of the interactive guide used via `tasklist`, would therefore report a leak on this machine every time. This is the round-2 blocker in concrete form: F8 can only be judged as "no process *absent from the baseline* remains", and the guide now takes a baseline before the user starts and a snapshot inside the still-open session.

**2026-09-16 — which `codex` the scenarios used (A12/F10 detail, no Codex call).** Two Codex installations exist on this machine, so the recorded version needs its binary named. The shell resolves `codex` to the pnpm shim `C:\Users\admin\AppData\Local\pnpm\codex`, which runs `…\pnpm\global\5\.pnpm\@openai+codex@0.154.0\node_modules\@openai\codex\bin\codex.js`; the only installed pnpm packages are `@openai+codex@0.154.0` and its `-win32-x64` sidecar, and `codex --version` reports `codex-cli 0.154.0`. The long-running PIDs 2524 / 37128 / 40976 still reference an `@openai+codex@0.153.4` path that no longer exists on disk — they are processes started on 09-14 and 09-15, before the package was upgraded in place, which is another reason a bare process-name check cannot judge F8. The Codex app's own binary (`…\AppData\Local\OpenAI\Codex\bin\12219cbfbcbddde7\codex.exe`, PIDs 34832 / 42444) is a separate installation that the skill never invokes.

**2026-09-16 — A7 (follow-up) PASSED; one Codex call (7 of 16).** Run by resuming A1's headless session (`--resume b42e681a-d65a-49da-89e5-edbeb8bd5ba4`) in the same project and asking to re-check C6, the one claim A1 had left as `investigate`.
- **No Codex-side resume (F-map):** the command line is a fresh `codex exec … -C 'D:/tmp/askcodex-live-09/a1' -m gpt-5.6-sol -c model_reasoning_effort="high"` with the full disable set — it contains neither `resume` nor `fork`. The continuity is carried in the prompt, not in a Codex session.
- **Carried claim in the fixed form:** `prompt.md` contains a "Context from Claude" section with exactly one line — `C6 [investigate] The profile caller interprets the resulting empty object as a missing user because it lacks an `id`. — Claude: not yet confirmed against src/pages/profile.js directly.` — under the instruction that carried claims appear as `<id> [<Claude's disposition>] <statement> — Claude: <reason>`, plus the follow-up framing (statuses required, `new-blocking` only for new blocking points) and the read-scope and secrets rules.
- **Status line in the reply:** `C6 [resolved] … — Updated disposition: adopt — …`, with the reason naming both the skill's own reading of `src/pages/profile.js:4-6` and Codex's independent confirmation; it states there were no new blocking claims and no open questions. The consultation type is reported as `follow-up`.
- **Cleanup and guarded files:** `rm -rf -- 'R:/Temp/ask-codex/run.3zxNMP'`; guarded files unchanged before and after.
- **Observation for the `/code-review` pass:** the disposition word was written as `**adopt**` (bold) inside the otherwise-fixed status line. Same class as ticket 08's advisory A2 — the fixed wording survives, but "plain text" does not hold; the regex graders tolerate markdown by design, so no eval catches it.

**2026-09-16 — A8 (timeout) PASSED on its timer behaviour; A8b (forced-stop probe) REFUTES F8. Two Codex calls (9 of 16).**
A8, headless with `EVAL_ASK_CODEX_TIMEOUT_MINUTES=1` and `sol:xhigh` on a deliberately heavy question:
- `Timeout override active: 1 minutes (staleness 10 seconds).` written as soon as the variable was read.
- At the check it judged liveness from the last event — `item.completed` (a ripgrep over `src/pages/profile.js`) 57 s old, past both the 10 s staleness threshold and T/2 = 30 s — stated that `AskUserQuestion` is unavailable in this session, took the recommended option, called `TaskStop`, and reported `Consultation stopped`, attributing nothing to Codex (no summary, no claims, no open questions). It also confirmed no `mcp_tool_call` line in `events.jsonl` and removed its run directory (`rm -rf -- 'R:/Temp/ask-codex/run.SilHR8'`). Guarded files unchanged.
- **F8 inside that session:** the prompt made it snapshot processes ~5 s after `TaskStop`, before its final reply. Against its own baseline (10 `codex*` processes) the snapshot showed **three new PIDs still alive** — 49108, 31216, 13432, all in the `@openai+codex@0.154.0` tree. They were still alive minutes later (they appear in A8b's baseline) and only disappeared afterwards.

A8b, the direct forced-stop probe with A2's exact flag set on the same slow prompt, stopped from this session with `TaskStop` — the mechanism the skill's stop path uses:
- `TaskStop` returned `Successfully stopped task`, yet the **whole process tree survived**: `sh.exe 32820 → node.exe 3952 (pnpm shim) → codex.exe 47640`, the last with exactly the argv I sent (`-o D:/tmp/askcodex-live-09/a8brun/last-message.json`). Still alive at +5 s and at +75 s.
- The run was not merely lingering, it was **still working**: `events.jsonl` grew from 788 bytes at stop time to 2579 bytes afterwards, while `last-message.json` never appeared. A "stopped" consultation therefore keeps running and keeps spending quota.
- I ended the chain myself with `Stop-Process -Force` on 47640, 3952 and 32820 and verified the snapshot returned to the pre-probe set. Guarded files unchanged.

**Measurement caveat (found later, does not change the verdict):** the quick PID diffs above used the regex `ProcessId\s*:\s*(\d+)`, which also matches inside `ParentProcessId :`, so those PID *sets* mixed in parent pids. The refutation does not rest on them: it rests on the targeted query that showed `codex.exe 47640` running my exact A8b argv (down to `-o …/a8brun/last-message.json`), parented by `node.exe 3952` and `sh.exe 32820`, all three alive at +5 s and +75 s, plus `events.jsonl` growing from 788 to 2579 bytes after the stop. Later comparisons (I3) anchor the match at line start.

**Verdict: F8 ("after a stop, no Codex process remains") is REFUTED live for the headless path.** This is a skill defect, not an environment quirk: the stop path in step 8 relies on `TaskStop` alone, which on Windows ends the harness task but not the `codex.exe` it spawned through the pnpm shim. Fix direction for a follow-up ticket: after `TaskStop`, kill the process tree explicitly (e.g. `taskkill /T /F /PID <pid>` on Windows, process-group kill elsewhere) and verify with a snapshot before reporting the stop. Acceptance 5 of this slice is therefore narrowed: F8 is recorded as **failing**, with I3 to show whether an interactive session behaves the same way.
It is not a slice stop condition (those cover the sandbox, MCP exposure and the guarded files), so the remaining scenarios continue.

**2026-09-16 — A9 (project path containing a space, F9) PASSED; one Codex call (10 of 16).** Run in `D:\tmp\askcodex-live-09\space proj`.
- **Quoting (the acceptance point):** every path carrying the space or the run directory appears quoted literally — `-C 'D:\tmp\askcodex-live-09\space proj'`, `-o 'R:\Temp\ask-codex\run.uagTNJ\last-message.json'`, `< 'R:\Temp\ask-codex\run.uagTNJ\prompt.md'`, `> '…\events.jsonl'`, `2> '…\stderr.log'`, and the directory commands `mktemp -d 'R:/Temp/ask-codex/run.XXXXXX'` and `rm -rf -- 'R:/Temp/ask-codex/run.uagTNJ'`. Nothing was split on the space and no path was left bare.
- **Consultation completed normally:** `gpt-5.6-sol` at effort `high` (config default model, skill's fixed effort), `MCP: all servers disabled for this consultation.`, four claims each with a disposition, and the printed reply passes `live-09-check.mjs` (4 claims, 0 open questions).
- **Evidence and hygiene:** no `mcp_tool_call` line in `events.jsonl`, run directory removed, guarded files unchanged before and after.

**2026-09-16 — A10 (MCP minimal-deny via the project config) PASSED; one Codex call (11 of 16).** The throwaway project carries `.claude/ask-codex.local.json` with `{"mcp_policy": "minimal-deny"}`, and the request confirmed that config by name and said what it widens, as the Confirmations rule requires.
- **Disable set (slice stop 4):** the `codex exec` argv carries exactly two disable definitions — `node_repl` and `cua_repl` — and nothing else. The guard listing before it was made with the same two `-c` overrides. No other server was disabled, and the policy was not silently widened beyond what the project config asked for.
- **Statement:** the reply carries the fixed line `MCP: minimal-deny — only node_repl and cua_repl disabled; other servers stay usable outside the sandbox.`
- **MCP tool calls:** none — `events.jsonl` contains no `mcp_tool_call` line even though four servers were reachable in this mode, so nothing outside the sandbox was invoked. (The residual risk recorded in the contract stands: those servers were *available* by instruction; this run simply did not use them.)
- **Cleanup and guarded files:** run directory removed (`rm -rf -- 'R:/Temp/ask-codex/run.R8pVIf'`); guarded files unchanged before and after.
- **Defect observation for the `/code-review` pass — language drift:** this run conducted the whole consultation and wrote its entire final answer in **Japanese**, although the session language is Traditional Chinese and nothing in the request was Japanese. A4 showed a milder form (one interim message in Japanese, Chinese final reply). The skill's fixed English wordings survived in both cases, so no grader catches it; the prose around them does not follow the session language.

**2026-09-16 — A11 (project-layer Codex MCP definition, declined) PASSED; zero Codex calls.** The project carries `.codex/config.toml` defining the server `project_notes` (`node scripts/notes-server.mjs`), and the request said in advance that anything needing confirmation counts as declined.
- **Nothing sent (slice stop 5):** the only shell commands in the entire run were `printenv CODEX_HOME`, the `cygpath` home lookup and `git rev-parse --show-toplevel`. No `codex` command of any kind, not even `codex mcp list`, and no temporary directory was created, so there was nothing to clean up.
- **The reply names the source precisely:** the file `D:\tmp\askcodex-live-09\a11\.codex\config.toml`, the server name `project_notes` and its command, plus the rule that a project-defined Codex MCP server needs explicit confirmation naming both before any `codex exec`. It then said what a confirmation would have to look like to proceed.
- **Attribution:** it offered its own reading of `src/user.js` and stated explicitly that this was its own view, not Codex's — nothing was attributed to Codex.
- **Guarded files:** unchanged before and after.

**2026-09-16 — automated set complete: A1–A12, 11 Codex calls of the 16 allowed (16 of 25 program-wide).** Passed: A1, A2 (with the network half reprobed), A3 (second attempt), A4, A5, A6, A7, A9, A10, A11, A12. A8's timer, stall detection, non-interactive stop rule and stop report all held. **A8b refutes F8** — the stop leaves the Codex process chain running (ticket 12), so acceptance 5 is narrowed to exclude F8. Two presentation defects go to the `/code-review` pass: markdown around the fixed disposition words (also ticket 08's advisory A2), and language drift — one interim message in Japanese in A4, and the whole of A10 in Japanese. Guarded files were compared before and after every single run and never changed after the one re-baselined app rewrite; every probe project stays under `D:\tmp\askcodex-live-09\` for deletion at the end. Remaining for this ticket: the interactive scenarios I1–I3, performed by the user.

**2026-09-16 — I1 (proactive consent): first attempt VOID, retest CONFIRMS the ticket-05 pause. Zero Codex calls in both.**
First attempt, void — the plugin was never loaded. After this run the user typed `/ask-codex:ask` in the same session and got `Unknown command: /ask-codex:ask`, and the transcript mentions `ask-codex` only twice in 187 lines (once as `package.json` content, once as that warning) with no plugin or skill metadata at all. So the "0 `Skill` invocations" below is most likely an environment fact, not skill behaviour, and **the attribution to ticket 05's known unreliability is withdrawn until the retest**. `--plugin-dir` is supported in interactive sessions (it is in `claude --help`) and the same flag worked in every headless scenario, so the retest starts by confirming that `/ask` completes to `ask-codex:ask` before anything else. The workspace was reset to the original fixture afterwards, because the first attempt's fixes had removed the bug entirely (the session had rewritten the whole error taxonomy — `HttpError`, `NetworkError`, `MalformedResponseError`, definitive-404 detection).
What the first attempt still shows, independent of the cause: The user drove a real fix loop in `D:\tmp\askcodex-live-09\interactive`: `fetchUser` swallowing timeouts, then two failed fixes, then an explicit statement that the failure could not be reproduced locally and that no further evidence was obtainable.
- **No proposal ever appeared.** Claude kept debugging on its own — it rewrote `user.js` to rethrow the last `TimeoutError`, rewrote `renderProfile` to catch it, added `[DIAG-09]` instrumentation, and asked its own clarifying questions instead of offering a consultation.
- **Transcript scan** (`…\.claude\projects\D--tmp-askcodex-live-09-interactive\6ad0cb21-….jsonl`, 185 messages): **0 `codex` commands, 0 `Skill` invocations**, 2 `AskUserQuestion` calls — both its own clarifying questions (how the failure was reproduced; whether the payload shape was measured or guessed), neither the consent block.
- **Diagnosis of the miss:** the skill was never loaded, so the proactive path could not run at all. That is exactly the root cause recorded when ticket 05 was paused — Claude does not reliably load the skill before deciding to propose — and this is its first confirmation outside the eval harness.
- **Safety:** held, though trivially so — with no `codex` command anywhere in the session there was nothing to consent to. The acceptance line "proactive consultations ask first" is therefore **recorded as observed, not verified**: the run shows the failure mode, not the consent block's three options.
- **Test-hygiene note:** the fixture has no reachable API (`api.example.internal` → `fetch failed` on attempt 0), so the reported symptom cannot be reproduced in the workspace at all. The user reported that honestly rather than insisting on a symptom the environment cannot produce; had they insisted, the run would have been void like A3's first attempt.

**Retest (session `21d017b1-…`, workspace reset to the original fixture first): the proposal never came, and this time the plugin was demonstrably loaded.** The session's attachment record lists the available skills and contains `ask-codex:ask: Consult OpenAI Codex (through the local Codex CLI) for an independent opinion …`, and there is no `Unknown command` line anywhere. Across 169 messages: **0 `Skill` invocations, 0 `codex` commands**, and one `AskUserQuestion` — again its own clarifying question ("what does the real API return for an existing user?", "how did you observe the failure?"), never the consent block. Through all three "still failing" rounds it kept debugging alone, rewriting `api.js` with a `NotFoundError`, unwrapping `{data:…}` envelopes in `fetchUser`, and separating "temporarily unavailable" from "user not found" in `renderProfile`.
- **Verdict:** the skill was available and the fix loop was exactly the shape the skill describes, yet no proactive consultation was proposed. This is the ticket-05 failure mode reproduced outside the eval harness, and it is the first live evidence for the pause the user decided on. Acceptance for the proactive path stays **recorded as observed, not verified**: the consent block's three options have still never been exercised live.
- **Safety:** held in both attempts, and again trivially — nothing ever reached a `codex` command, so there was nothing to consent to. No run directory was created, and the guarded files were unchanged throughout.
- **Reader's note:** this machine also has an unrelated `codex` plugin installed (its `codex:setup` command appears in the same skill listing). Nothing in these runs came from it.
