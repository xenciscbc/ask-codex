# 11 — MCP policy configuration and setup

**What to build:** The user can change the MCP policy that ticket 01 applies by default. A user-level config (`~/.claude/ask-codex.json`) and a project-level config (`.claude/ask-codex.local.json`, project values override user values per key) choose `mcp_policy` = `allowlist` (default) or `minimal-deny` (only `node_repl` and `cua_repl` disabled) and list allowed servers in `mcp_allow`. `/ask-codex:setup` lists the MCP servers Codex sees (`codex mcp list --json` run in the project, so global + project Codex config), takes the scope, mode and servers from the user's message or asks for them (`AskUserQuestion`, multi-select batched when more than four; plain text when unavailable), writes the chosen file, and shows the result — never writing without the user's answers. A project-level config wider than the default is confirmed once per session before use (F12). A project-defined server that the policy would keep enabled (only in the project listing, or any definition field different from the neutral listing) needs confirmation once per session, otherwise it is disabled (F12b a/b). A project-layer Codex `mcp_servers` definition (rule (c), any table or key whose path starts with `mcp_servers`, quoted or not) is asked about once per session naming each file, server and command, and the consultation is aborted on decline. Every consultation's result header states the effective MCP policy with a fixed statement. See spec: stories 24–32 and the confirmation-scope contract; ADR-0003; execution contract `slice-11.md` (rev 3).

**Confirmation scope (user decision 2026-09-15, Q1-A, tied to item type):** a confirmation written in a user message — including the invoking message — counts only for the items it explicitly names, and only for its own kind: the project ask-codex config widening (must name this project's ask-codex config and the widened mode or each widened server), a project-defined server (must name the server and say it is project-defined or its definition differs), or a project Codex MCP definition (must name `.codex/config.toml` and each server). A confirmation of one kind never covers another kind, even for the same server name; a generic "I confirm" confirms nothing; a decline always wins.

**Header statements (exact; reused by ticket 05's consent line):**
- default: `MCP: all servers disabled for this consultation.`
- allowlist with servers: `MCP: allowed — <names>; all other servers disabled.`
- minimal-deny: `MCP: minimal-deny — only node_repl and cua_repl disabled; other servers stay usable outside the sandbox.`

**Blocked by:** 01 — Manual consultation tracer bullet (resolved).

**Status:** ready-for-agent

- [ ] Config resolution: no file → allowlist with an empty list; project overrides user per key; invalid values rejected with a message and the default applied.
- [ ] Setup skill writes only after the user's answers, only the chosen scope's file, and shows the resulting policy.
- [ ] `skills/ask/SKILL.md` contains the typed confirmation-scope rule and the three header statements verbatim; rule (c) covers any table or key whose path starts with `mcp_servers`, quoted or not (A3).
- [ ] Evals (all twelve pass; config source, prompt addition and expected stub records per `slice-11.md`): `policy-default-allowlist`, `policy-allowlist-keeps-listed`, `policy-minimal-deny`, `project-widening-confirm`, `project-redefined-allowed`, `project-env-redefined`, `project-config-table`, `project-layer-decline-aborts`, `project-layer-bare-table`, `setup-writes-config` (user scope: the Write targets `.claude/ask-codex.json` with the chosen policy, no shell workaround, outcome reported — the eval harness seals the child's `home/.claude` read-only, so the write itself cannot succeed there), `setup-project-scope` (setup writes the project config, then a confirmed consultation in the same run keeps comfyui), `pre-confirm-mismatch`.
- [ ] Header statements: in `policy-default-allowlist`, `policy-allowlist-keeps-listed`, `policy-minimal-deny` and `setup-project-scope`, the result header states the effective MCP policy using the exact statements above.
- [ ] No acceptance step writes the real `~/.claude/ask-codex.json`; the skill may read it. Live runs check its state before run A, after run A, and after run B, and stop before any Codex call if it changed.
- [ ] Live (≤ 3 Codex calls; Windows headless `claude -p --plugin-dir`, file-tool writes limited to the temp project and temp base): run A — `/ask-codex:setup` with project scope, allowlist, `comfyui` in the prompt (no Codex call); run B — one consultation with the F12 confirmation in the prompt and a question answerable only by `comfyui.server_info`, expecting exactly one MCP tool call (`comfyui.server_info`) and disable definitions for every other server; temp project under `D:\tmp\<subdir>` deleted afterwards.
- [ ] Fresh verifier CONFIRMED on F-map rows F1-config, F1-claims (result header per mode + doc updates), F12, F12b (11 part, including the typed confirmation-scope rule and `pre-confirm-mismatch`), and A3.

## Comments

**2026-09-15 — eval fact (from the ticket-01 harness spike).** `AskUserQuestion` is unavailable inside `claude plugin eval` children. Evals that expect "AskUserQuestion before the first `exec`" assert instead: zero `codex exec` before confirmation plus an `llm` grader that the reply asks for the confirmation (naming the servers/commands); confirmed paths are seeded via `context.history_file`. The setup skill's multi-select flow is exercised in live acceptance; its eval (`setup-writes-config`) seeds the user's answers in history.

**2026-09-15 — deferred from the ticket-01 verifier (advisory A3, P3).** Rule (c) wording in `skills/ask/SKILL.md` step 2 names `[mcp_servers.<name>…]` table headers and keys starting with `mcp_servers`, but does not spell out a bare `[mcp_servers]` table with sub-keys or quoted keys (e.g. `mcp_servers."name"`). When this ticket adds the confirmation path, widen the wording to "any table or key whose path starts with `mcp_servers`, quoted or not" and add a scaffold variant using `[mcp_servers]` + `name.command = …`.

**2026-09-15 — slice-11 readiness.** Three plan-verifier rounds (REVISE ×3) led to: per-case config source and pre-confirmation sentences instead of `history_file` seeding (the user's own invoking message is a valid confirmation source); the typed confirmation-scope rule (user decision Q1-A) with the mismatch case `pre-confirm-mismatch`; the earlier per-mode consent-line eval replaced by per-mode result-header statements (the consent line itself belongs to ticket 05, which reuses the statement); a guarded live check (state checks of the real user config before and between runs, file-tool writes limited to temp paths); ticket-11 test cap raised to $8 (user decision Q2-1). The user approved execution after the closing review's single blocker (typed scope) was fixed.

**2026-09-15 — red and first green pass.** Red on four representative cases ($0.79): all failed on the new behaviour as expected (comfyui still disabled, no header, exec despite a redefined server, `/ask-codex:setup` missing). First green pass, eleven cases ($2.55): 1.0 for policy-default-allowlist, policy-minimal-deny and all five F12b / rule-(c) stop cases (project-redefined-allowed, project-env-redefined, project-config-table, project-layer-decline-aborts, project-layer-bare-table). Findings: (1) keeps-listed missed the verbatim header — step 10 now puts the MCP statement on its own line, copied exactly; (2) project-widening-confirm and pre-confirm-mismatch stopped correctly but before any action the `skill-fired` marker recognised — the marker now also accepts reading the ask-codex config; (3) **eval fact:** the harness seals the eval child's `home/.claude` read-only (placeholder files, writes rejected), so setup cannot write the user-level config inside an eval; the agent then tried a shell heredoc to write it anyway. Dispositions per stop condition (5): `setup-writes-config` checks the write attempt, the absence of any shell workaround, and the reported outcome; the end-to-end setup → consultation path moved to the new case `setup-project-scope`; the setup skill now stops and reports when its write fails. Grader regexes self-checked before rerunning (a first draft of `no-shell-workaround` missed escaped quotes and was fixed).

**2026-09-15 — second pass (seven affected cases, $1.62).** 1.0 for policy-allowlist-keeps-listed, policy-default-allowlist, policy-minimal-deny, pre-confirm-mismatch, project-widening-confirm and setup-writes-config. **Second eval fact:** the harness also denies writes under the workspace's `.claude/` ("don't ask" mode, protected path), so `setup-project-scope` could not write `.claude/ask-codex.local.json`; the setup skill stopped and reported it without any workaround, as required. Disposition (stop condition 5): `setup-project-scope` checks the write attempt (target file and policy), the absence of a shell workaround and the reported outcome; reading a project config is already covered by policy-allowlist-keeps-listed, and the real setup → consultation path by the live check. In interactive sessions Claude Code asks the user before writing under `.claude/`, which the README will mention. The live script creates the project config itself if run A's write is blocked, and records that fallback.

**2026-09-15 — eval status: all twelve cases 1.0 on the current graders.** setup-project-scope rerun with the attempt-based graders: 1.0 ($0.11). The five F12b / rule-(c) stop cases passed in the first green pass; they stop before step 10, so the later header-wording change does not touch their path. Ticket-11 test spend ≈ $5.07 of $8 before the live check.

**2026-09-15 — live check (Windows, headless `claude -p --plugin-dir`, model sonnet; 1 real Codex call).** Permissions for both runs: `--permission-mode acceptEdits --add-dir R:/Temp --allowedTools Bash Read Glob Grep Skill` (file-tool writes only to the temp project `D:\tmp\askcodex-live-11` and the temp base). Real `C:\Users\admin\.claude\ask-codex.json`: absent before run A, after run A, and after run B; no backup needed; zero Write/Edit calls to that path in either transcript. **Run A** (`/ask-codex:setup Scope: project. Mode: allowlist. Allow: comfyui.`): the setup skill listed servers and attempted the Write to `.claude/ask-codex.local.json`; the headless permission system refused it as a sensitive file; the skill reported that and asked for permission — no workaround. The test script created the project config (recorded fallback). **Run B** (consultation with the F12 confirmation in the prompt): neutral and project listings via `env -C`, guard, then `codex exec … -m gpt-5.6-sol -c 'model_reasoning_effort="high"'` with disable definitions for blender, codex_app, cua_repl, node_repl and pencil — **not** comfyui — plus `--disable apps`; events contained exactly one MCP tool call, `comfyui.server_info` (started + completed, returned ComfyUI's status); the result header read `MCP: allowed — comfyui; all other servers disabled.` verbatim; run dir removed. No leftover comfy/codex/python processes; the temp project was removed (first attempt hit a transient "busy"). Live Claude cost ≈ $0.80 → ticket-11 test spend ≈ $5.87 of $8; live Codex calls 1 of 3.