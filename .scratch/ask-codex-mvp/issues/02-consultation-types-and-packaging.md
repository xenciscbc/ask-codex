# 02 — Consultation types and packaging

**What to build:** Claude infers the consultation type from context — second opinion, diagnosis, targeted check, or technical question — for every consultation, and packages it accordingly: with-stance for second opinion and targeted check (the Plan / implementation / diff is included and Codex is told to verify independently and challenge it), blind for diagnosis and technical question (question and evidence only; for diagnosis every failed attempt and its result is included, Claude's hypothesis is not). Two more manual entry points work: `/ask-codex:ask` with no question (Claude infers the question from the conversation and sends it, showing a one-line summary; only if nothing sensible can be inferred does it ask the user), and a verbal request such as "ask codex about this", which counts as a manual consultation with no consent prompt. See spec: user stories 2–4, 14–18.

**Blocked by:** 01 — Manual consultation tracer bullet.

**Status:** ready-for-agent

- [ ] One English prompt template per consultation type, each carrying the read-scope instruction and the with-stance or blind framing.
- [ ] Diagnosis prompts include failed attempts and their results and exclude Claude's root-cause hypothesis.
- [ ] Targeted-check prompts carry Claude's specific concern and do not ask for a general review.
- [ ] Empty invocation: the question is inferred and sent with a one-line summary shown; if no sensible question exists, Claude asks instead of sending.
- [ ] Verbal requests to consult Codex trigger the skill and never show a consent prompt.
- [ ] Eval cases pass: manual without question; verbal request; diagnosis case (stdin recorded by the stub contains the failed attempts and not the hypothesis seeded in history); second-opinion case (stdin contains the Plan and the challenge instruction).
- [ ] Every template keeps the read-scope + in-project secret exclusion, MCP-lookup-only, and data-not-instructions requirements from ticket 01.
- [ ] Eval `secret-not-sent` (F3-secrets): a fake token seeded in history is absent from the stub-recorded stdin.

## Comments

**2026-09-15 — slice-02 readiness.** Three plan-verifier rounds (REVISE ×2, then a closing REVISE with one blocker) led to: per-type framing files with fixed marker lines and marker checks on the stub-recorded stdin; blind packaging that withholds the user's stance as well as Claude's; a shared conversation fixture seeded through `context.history_file`; a history-load probe with a no-fixture control; an empty-invocation case that must ask (step 0, before any temp dir or `codex` call); graders with no `flags` (case-insensitivity spelled out); a red-phase leak control proving the negative stdin grader fires inside the harness; a plain `fetchUser` scaffold (no retry loop) for the diagnosis and secret cases so the retry-attempt grader can only pass from the conversation. The user approved execution after the closing review's blocker was fixed in the contract.

**2026-09-15 — eval facts.** `context.history_file` is passed to the eval child as `--resume <file>` (path resolved inside the case directory; must be a plain single-link file), so it is a Claude Code session transcript. The fixture is a hand-written two-line transcript (one user message with the problem, the two failed attempts, the hypothesis and a fake key; one neutral assistant reply) — a real session transcript was not used because it embeds the local system prompt, user settings and email. Regex graders default to `target: last_message`; `llm` graders default to `focus: last_message`. The offline check `evals/_harness/ticket02-patterns.test.mjs` (163 checks, pass) caught the 10-second pattern missing "10 second"/"10-second"; the pattern was widened before any paid run.

**2026-09-15 — history probe ($0.08).** `history-probe` (fixture, empty workspace, no tool calls) scored 1.0 — the final reply named `fetchUser`; `history-probe-nofixture` failed `names-fetchuser` (judge FAIL ×3). History is loaded and the grader separates the two, so diagnosis-blind, secret-not-sent and manual-without-question run as specified (no stop-(5) fallback).

**2026-09-15 — invalid first red attempt ($0.52, discarded).** The first red runs were launched without `--allow-tools Bash Write`; the harness then withholds Bash and Write from the agent ("not granted (missing --allow-tools grant): Bash, Write"), so the skill never ran and every exec grader failed for that reason, not for the missing behaviour. Those results are discarded; `run-evals.sh` now documents the grant in its usage line, and the red cases were rerun with it.

**2026-09-15 — red on the ticket-01/11 skill ($1.50, with the grant).** diagnosis-leak-control 0.85: `no-hypothesis` FAIL (the negative stdin grader fires inside the harness — B3 proven) and `marker-diagnosis` FAIL. diagnosis-blind 0.85: same two failures (the user's hypothesis reached Codex; no type marker). technical-question-blind 0.83: `marker-technical-question` FAIL and `no-leaning` FAIL (the user's leaning reached Codex). secret-not-sent 1.0: the ticket-01 ground rule already kept the fake key out; the case stays as a regression guard for the new packaging step. Each red run cost about $0.35–0.40 (above the $0.30 estimate); slice-02 spend so far ≈ $2.10 of $6.

**2026-09-15 — first green pass (nine cases, $2.90).** 1.0 for verbal-request, diagnosis-blind, second-opinion-with-stance, technical-question-blind, secret-not-sent, manual-without-question-nothing-to-infer and manual-with-question (ticket-01 regression). Two failures, both dispositioned:
- targeted-check 0.92: `concern-sent` missed — the concern reached Codex reworded, not verbatim. Fix (skill): the step-0 table now says the concern is quoted verbatim, never rephrased (the contract required "the concern verbatim").
- manual-without-question 0.11: the agent inferred the question correctly (and noted it would leave out the hypothesis and scrub the key) but refused to consult because the empty workspace lacked the `src/user.js` the conversation talks about — reasonable behaviour, a test-design flaw. Fix (case): the case now uses the plain `fetchUser` scaffold and grades only conversation-only tokens (the 10-second / retry attempts, absent from that scaffold per the offline isolation check); `inferred-fetchuser` was dropped. This is the second option the closing review allowed for B2. Offline check rerun: 162 passed.
Slice-02 spend ≈ $5.00 of $6 before the two reruns.

**2026-09-15 — green complete ($0.76 for the two reruns).** targeted-check 1.0 (the concern reached Codex verbatim, own marker only, no general-review wording) and manual-without-question 1.0 (a failed attempt from the seeded conversation reached Codex; the judge saw the inferred question shown). All nine green cases are now 1.0. Byte coverage: the only skill edit after the first green pass is the targeted-check cell of the step-0 table (verbatim concern); the seven cases that passed before it do not use that type. Narrowed: the no-fixture control was not rerun for the revised manual-without-question (the extra run would exceed the $6 cap); its conversation-only tokens are proven absent from its scaffold by the offline isolation check, and the probe pair already showed the fixture is what makes the conversation visible. Slice-02 spend ≈ $5.76 of $6; no live Codex calls.

**2026-09-15 — eval fact: resumed runs write a transcript into the case directory.** A case with `context.history_file` leaves the child's full session transcript (`<sessionId>.jsonl`, here 330 KB with system-prompt content) next to `history.jsonl`. It was found before commit, deleted, and `.gitignore` now excludes `evals/*/<uuid>.jsonl`. The committed `history.jsonl` files are byte-identical to the hand-written fixture.
