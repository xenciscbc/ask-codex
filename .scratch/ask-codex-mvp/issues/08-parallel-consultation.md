# 08 — Parallel consultation

**What to build:** In a manual consultation the user can name two different models, each with an optional effort (e.g. `astra:high, sol`), and receive two independent opinions. Claude runs one background Codex run per model with identical packaging, neither seeing the other's output, and presents a single merged result grouped into consensus, solo claims, and divergences — each claim tagged with its source model and Claude's disposition, and each divergence explaining which side Claude adopts and why. More than two models or the same model twice is refused with a clear message and no Codex call. If one model fails, the other's result is presented normally with a note naming the failed model and reason. Both runs share one timeout timer: at expiry Claude reports which model is done and asks only about the ones still running, holding finished results until everything completes or the user stops the rest. Parallel is manual-only, except when the session-scoped model override names two models. See spec: user stories 36–44, 64.

**Blocked by:** 03 — Model aliases and effort rules; 04 — Failure handling; 07 — Timeout monitoring.

**Status:** resolved

- [ ] Two distinct models resolve via alias rules, each with its own effort computed by the consultation-effort rules.
- [ ] Two separate background runs with separate temporary directories; identical packaged question.
- [ ] Merged presentation uses the consensus / solo claims / divergences grouping with model tags and dispositions; every divergence has an adoption rationale.
- [ ] Over-limit or duplicate-model requests are refused with no Codex call.
- [ ] Partial failure presents the surviving opinion (without grouping) plus the failure note.
- [ ] Shared timer behaviour as described; no half-result is shown and later revised.
- [ ] Proactive consultations run in parallel only when the session override names two models.
- [ ] Eval cases pass: `astra, sol` → two `codex` calls with `gpt-6-astra`/`medium` and `gpt-5.6-sol`/`high`; divergent stub replies → `llm` grader checks grouping and rationale; three models → zero `codex` calls; one stub run failing → surviving result plus failure note.

## Comments

**2026-09-16 — slice-08 readiness.** Round 1 REVISE (2 blockers) led to: per-model argv graders on `exec-argv.<slug>.json` (full disable set `count:5`, `-s read-only`, `--disable apps`) plus `no-violations`, because two runs in one project would otherwise leave only the last call's argv; and a fixed `Parallel check: done — …; still running — …` line with regex graders and an offline paraphrase test, plus a `--keep-temp` transcript excerpt for case 5 so the verifier can check that no claim is shown before the check. Advisories taken: "one `codex exec` per consultation" becomes "per model"; stdin files must not contain the other model's reply; two temp dirs and two background runs counted; fixed refusal line; a two-model metachar example in the reference token rule. Round 2: READY. Narrowing: proactive two-model parallel (session override naming two models) is written in the skill but not claimed as verified while ticket 05 is paused. Execution starts once ticket 06 is confirmed.

**2026-09-16 — entry gate, stub, reference rule and offline checks.** Ticket 06 is resolved (verifier CONFIRMED, d8102ca). The stub now reads `exec.by_model["<slug>"]` for calls with that `-m` (mode, reply, durations), appends one `exec.sentinel` line per call and writes `exec-argv.<slug>.json` / `exec-stdin.<slug>.txt` next to the single-run files (other tickets' graders unchanged); `stub-modes.test.mjs` 40 passed, 0 failed. The reference rule gained `readModels()` (two models → parallel; three → refused; the same model twice → refused; `astra, sol;touch` → invalid; a question such as "Why, exactly, …" stays a question); its self-check 18/0 and `ticket03-patterns.test.mjs` 69/0. Five ticket-08 cases generated (Codex-home seeding behind ticket 03's HOME guard; ticket-01 workspace). `ticket08-graders.test.mjs` 32 passed, 0 failed (per-run argv graders count 5 disable definitions and fail at 4; every fixed-wording regex fails a paraphrase; the swapped check line fails). All earlier offline checks and `claude plugin validate` pass.

**2026-09-16 — red on the ticket-06 skill ($0.96, no "not granted" notice).** parallel-two-models 0.75: the old skill improvised two runs (each carrying the full disable set — the per-run contract graders passed) but produced no `Consensus` / `Solo claims` / `Divergences` headings, no model tags, no `Adopted:` and failed the llm grouping grader. parallel-three-refused 0.75: no `codex` command, but no fixed refusal line. parallel-one-fails 0.13: no consultation at all. Every red case fails on the new behaviour.

**2026-09-16 — first green pass ($2.79; case 5 run with `--keep-temp`).** 1.0 for parallel-three-refused, parallel-duplicate-refused, alias-sol (ticket-03 regression) and manual-with-question; every per-run contract grader passed where two runs happened. Three wording misses, behaviour right (all llm graders passed): parallel-two-models 0.93 — tags written as the aliases `[astra]` / `[sol]` instead of the full slugs; parallel-one-fails 0.94 — `**Failed model:** gpt-5.6-sol` (bold between the colon and the slug); parallel-shared-timer 0.89 — the same bold on the stop line, and the `Parallel check:` line was not restated in the final reply. Fixes: the skill requires full model slugs in tags, plain text (no bold) for the `Failed model:` and `Parallel check:` lines, and restating every check line in item 1; the `Failed model:` and `Parallel check:` regexes tolerate markdown around the fixed words (not a different model or order); offline samples added. The case-5 transcript excerpt was written to `.scratch/ask-codex-mvp/evidence/parallel-shared-timer.txt` and the kept directory deleted; all seven cases are rerun (case 5 kept again for a fresh excerpt).

**2026-09-16 — green on the final bytes: all seven cases 1.0, no "not granted" notice.** parallel-two-models (two runs each carrying the full contract; `Consensus` / `Solo claims` / `Divergences`, full-slug tags, `Adopted:`), parallel-three-refused and parallel-duplicate-refused (fixed refusal line, zero `codex` calls), parallel-one-fails (survivor presented, `Failed model: gpt-5.6-sol`), parallel-shared-timer (TaskStop used, `Parallel check: done — gpt-5.6-sol; still running — gpt-6-astra`, `Failed model: gpt-6-astra — stopped …`; run with `--keep-temp`, its assistant texts extracted to `.scratch/ask-codex-mvp/evidence/parallel-shared-timer.txt`, kept directory deleted), and the regressions alias-sol (ticket 03) and manual-with-question (ticket 01). No skill edit after these runs started. No live Codex calls.

**2026-09-16 — evidence check tightened.** The first excerpt header contained the phrase "Parallel check:", so the offline check (which split at the first occurrence) would have passed trivially. The test now splits at the first line that *starts* with `Parallel check:` and requires that line to exist; the header was reworded. Proven both ways: the real excerpt passes, a claim inserted before the check line fails, and an excerpt without the check line fails. `ticket08-graders.test.mjs` 39 passed, 0 failed. The extraction script is kept at `.scratch/ask-codex-mvp/plan/extract-excerpt.sh`.

**2026-09-16 — outcome verifier (commit effd9ab): REFUTED (one P2) — fix/reverify pass 1.** Claims 1, 2, 4 and 5 pass (byte coverage confirmed; the markdown-tolerant regexes still reject a wrong model or order — 14 probes). P2 F1: in the only run that reached a timer check, the check message was prose; the fixed `Parallel check:` line appeared only when restated in the final reply, and both the `check-line` grader (last message) and the excerpt check ("a line starts with it") accepted that. Advisories: A1 (P3) `count:5` does not prove five *distinct* servers (a duplicate plus a missing one also counts 5); A2 (P4) `failed-line` does not require the reason and `temp-cleanup` does not prove both run dirs removed; A3 (P4) the excerpt check misses paraphrased half-results (none present — the verifier read texts 1–4).
Dispositions: F1 FIX — step 8 now says the message written at a check *starts* with the `Parallel check:` line, before `TaskStop`, asking or waiting; the offline excerpt check now requires the first such line in an assistant text before the final reply. A1 FIX (bounded) — the stub records a repeated disable definition as a violation, so `no-violations` plus `count:5` proves five distinct servers (stub test sample added). A2, A3 DEFER to the final suite / `/code-review` pass. All seven cases are rerun (case 5 kept for a fresh excerpt), then one fresh verifier.

**2026-09-16 — pass-1 offline proof.** On the old excerpt (from effd9ab's green run) the tightened check fails — "the check line appears in a message before the final reply" — which is exactly F1, so the check now discriminates. `stub-modes.test.mjs` 41 passed, 0 failed (a repeated disable definition is logged as a violation). All other offline checks and `claude plugin validate` pass. The seven cases are being rerun on the fixed bytes.

**2026-09-16 — pass 1 did not fix F1 ($2.75, no "not granted" notice).** Six of the seven cases scored 1.0; parallel-two-models scored 0.96, failing only `two-temp-dirs` (`mktemp -d` counted as Bash *calls*, `min: 2`, and this run created both directories in one command). The fresh case-5 excerpt was extracted to `.scratch/ask-codex-mvp/evidence/parallel-shared-timer.txt` and `ticket08-graders.test.mjs` fails on it: "the check line appears in a message before the final reply" — F1 again. The kept trace shows why the reworded step 8 has nothing to bite on: the whole check happened inside one assistant message with **no text block at all** — `TaskOutput` (block 60s) → `Bash` (event age) → `ToolSearch AskUserQuestion` → `TaskStop` — and the first text afterwards was "Now reading sol's reply before cleanup." The rule was written as a property of "the message you write"; the model simply wrote none.
Test-design fixes first, so the eval itself can see the defect instead of an offline excerpt: `claude plugin eval` graders accept `target: trace` (whole session, one JSON line per message) — new grader `parallel-shared-timer/graders/check-line-before-stop.md` (regex over the trace, `Parallel check:[\s\S]*"name":"TaskStop"`) requires a check line earlier in the trace than the `TaskStop` call; calibrated on the kept trace, where only the real call matches `"name":"TaskStop"` and both `Parallel check:` occurrences come from the final reply, so it is red on this behaviour. `two-temp-dirs` in all three parallel cases is relaxed to a trace count (`mktemp -d`, `count:2`): one run directory per model is the contract, two separate commands never were. The kept directory is deleted. Red run of parallel-shared-timer and parallel-two-models on the unchanged skill is in flight; the step-8 rewording follows it.

**2026-09-16 — red proven, then green on the final bytes (pass 2; $1.11 red + $5.12 green, no "not granted" notice).** Red on the unchanged skill: parallel-shared-timer 0.95, failing only `check-line-before-stop` ("pattern not found in trace"), and the relaxed `two-temp-dirs` passed in both cases — so the new grader discriminates on exactly F1. parallel-two-models scored 0.96 in that run, failing `h-divergences` (no `Divergences` heading) — a second, nondeterministic gap in the merge presentation, since the same case had passed that grader an hour earlier.
Skill fixes: step 8 is now a prohibition tied to the action — "never stop a run, ask the user, or start another wait without having written this line for that check first", written as the first text of the message in which the check is made, with "making a check in a message with no text at all breaks this rule" and a next-message fallback; step 10 requires all three headings (`Consensus`, `Solo claims`, `Divergences`) every time, in order, with `none` under an empty one. An interim absolute form ("never call `TaskStop` in a message that does not already carry this line") was green but stricter than both the grader and the observed behaviour (the stop decision lands in a later message), so it was reworded to match what is actually enforced, and every case was rerun on those final bytes.
Green on the final bytes: all seven cases 1.00 — parallel-two-models, parallel-three-refused, parallel-duplicate-refused, parallel-one-fails, parallel-shared-timer (`--keep-temp`), and the regressions alias-sol (ticket 03) and manual-with-question (ticket 01). The kept trace shows the order the contract requires: `Parallel check: done — gpt-5.6-sol; still running — gpt-6-astra.` opens the message that makes the check and judges liveness, the stop decision follows, and only then comes `TaskStop`; the line is restated in the final reply. The fresh excerpt (12 assistant texts) is in `.scratch/ask-codex-mvp/evidence/parallel-shared-timer.txt` and the kept directory is deleted. Offline: `ticket08-graders.test.mjs` 40/0 on the new excerpt; stub-modes 41/0, model-token-rule 18/0, codex-call-regex PASS, ticket02 162/0, ticket03 69/0, ticket04 6/0, ticket05 43/0, ticket06 28/0, ticket07 14/0; `claude plugin validate` passes. No live Codex calls. Deferred (unchanged): A2/A3 from the verifier, and same-message enforcement of the check line is not graded — the grader requires the line earlier in the trace than the `TaskStop` call.

**2026-09-16 — outcome verifier (commit eb87a93): CONFIRMED — ticket resolved.** Byte identity checked independently (SKILL.md mtime 23:48:29Z precedes the first green run at 23:48:53Z; grader mtimes precede all seven; each `aggregate-result.json` records the same grader set and the relaxed `two-temp-dirs` config), all five acceptance conditions verified from the recorded results and the final messages, and every offline suite rerun by the verifier itself (ticket08 40/0, ticket02 162/0, ticket03 69/0, ticket05 43/0, stub-modes 41/0, and the rest). The refuted point is now discriminated: on the red run (`results/2026-09-15T23-25-23-579Z`, same case and graders, unchanged skill) `check-line-before-stop` was the only failure while `check-line` (last message) and `used-taskstop` passed, so the grader greens only when the assistant itself wrote the line before `TaskStop`; in the green run the check line opens the message that judges liveness, the stop decision follows, and texts 1–11 contain no claim, summary or grouping.
Advisories, all non-blocking and all DEFERRED (a P3/P4 finding must not change a CONFIRMED candidate): A1 (P3) each green case ran once although this suite is known to be nondeterministic — recheck with `--runs 3` in the final suite; **A2 (P3) narrows the claim**: step 10 requires `Failed model:` and `Parallel check:` in plain text, but the graded runs wrote `**Failed model:**` / `**Parallel check:**` in bold (the regexes tolerate markdown by design from pass 1), so "plain text, no bold" is *not* verified — either add a regex forbidding the bold form or relax the wording, in the `/code-review` pass; A3 (P4) the identical packaged question is proven only indirectly (both stdin files mention `fetchUser`, neither contains the other model's claim) — no byte comparison of the two stdin files; A4 (P4) the new grader couples to trace order, not to one message, and `two-temp-dirs` is a whole-trace count; A5 (P4) the kept traces are deleted, so the excerpt cannot be re-derived — keep a copy of `trace.jsonl` next to the excerpt next time (ticket 09 already prints its evidence before cleanup).
Unproven and recorded as such: proactive two-model parallel consultation (ticket 05 paused), same-message enforcement of the check line, the plain-text requirement (A2), the reason in `Failed model:`, and removal of *both* run directories (`temp-cleanup` proves at least one).

**2026-09-16 — advisory A2 closed in the `/code-review` pass.** Rather than add graders forbidding emphasis, the skill was aligned with what is actually enforced and observed: the parallel reply, the `Failed model:` line and the follow-up status line now say markdown emphasis around the fixed words is allowed, while the words, their order and the values are not. Ticket 09 saw all three variants bolded live (`**adopt**`, `**Failed model:**`, `**Consultation stopped**:`), and the regexes have tolerated markdown by design since pass 1, so the old "plain text (no bold)" wording promised something nothing checked. Nothing else in the ticket's claim changes; the narrowing that "plain text" was unverified is now moot because the requirement is gone.

**2026-09-16 — this ticket's parallel claim is narrowed by [ticket 15](15-parallel-check-line-unreliable.md).** The CONFIRMED above rested on a **single** green run of `parallel-shared-timer`, which is exactly what its verifier's advisory A1 warned about. Resampling with `--runs 3` later measured **2 of 3**: one run wrote no `Parallel check:` line anywhere before `TaskStop`. Read claim 5 of this ticket as "holds in about two runs of three", not "holds". `parallel-two-models` resampled 3/3, so the merge presentation itself is stable.
